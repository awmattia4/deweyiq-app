---
phase: 12-subscription-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/subscriptions.ts
  - src/lib/db/schema/index.ts
  - src/lib/db/schema/relations.ts
  - src/lib/stripe.ts
  - package.json
autonomous: true
requirements: [SUB-01]

must_haves:
  truths:
    - "subscriptions table exists with org_id unique FK, Stripe IDs, status/tier enums, period dates, pool count tracking, and failed payment fields"
    - "subscription_status enum covers: trialing, active, past_due, canceled, unpaid, incomplete, paused"
    - "billing_tier enum covers: starter, pro, enterprise"
    - "RLS: all org members can SELECT; only service role can INSERT/UPDATE (webhook-driven)"
    - "Stripe client singleton exported from src/lib/stripe.ts with locked API version"
    - "TIER_LIMITS and TIER_PRICE_IDS config constants exported from stripe.ts"
    - "stripe npm package installed"
  artifacts:
    - path: "src/lib/db/schema/subscriptions.ts"
      provides: "Subscriptions table schema with enums and RLS policies"
      contains: "subscriptions"
    - path: "src/lib/stripe.ts"
      provides: "Stripe client singleton and tier configuration"
      exports: ["stripe", "TIER_LIMITS", "TIER_PRICE_IDS"]
---

<objective>
Create the subscriptions database table, Stripe client module, and tier configuration — the foundation that all other billing plans depend on.

Purpose: Establish the data model for tracking subscription state per org and the Stripe integration utilities.
Output: Drizzle schema with RLS, Stripe client singleton, tier config constants.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-subscription-billing/12-CONTEXT.md
@src/lib/db/schema/index.ts
@src/lib/db/schema/relations.ts
@src/lib/db/schema/orgs.ts
@src/lib/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install stripe package and create subscriptions schema</name>
  <files>
    package.json
    src/lib/db/schema/subscriptions.ts
    src/lib/db/schema/index.ts
    src/lib/db/schema/relations.ts
  </files>
  <action>
    Install Stripe SDK:
    ```bash
    npm install stripe
    ```

    Create `src/lib/db/schema/subscriptions.ts`:
    - Define `subscriptionStatusEnum` pgEnum with values: trialing, active, past_due, canceled, unpaid, incomplete, paused
    - Define `billingTierEnum` pgEnum with values: starter, pro, enterprise
    - Define `subscriptions` pgTable with columns:
      - id: uuid PK with defaultRandom
      - org_id: uuid FK to orgs.id with cascade delete, UNIQUE constraint
      - stripe_customer_id: text, notNull
      - stripe_subscription_id: text, nullable (null during trial-only state)
      - stripe_price_id: text, nullable
      - status: subscriptionStatusEnum, notNull, default "trialing"
      - tier: billingTierEnum, notNull, default "starter"
      - current_period_start: timestamp with timezone, nullable
      - current_period_end: timestamp with timezone, nullable
      - cancel_at_period_end: boolean, notNull, default false
      - trial_ends_at: timestamp with timezone, nullable
      - pool_count: integer, notNull, default 0
      - pool_limit: integer, notNull, default 79
      - failed_payment_count: integer, notNull, default 0
      - last_payment_error: text, nullable
      - grace_period_ends_at: timestamp with timezone, nullable
      - created_at: timestamp with timezone, defaultNow, notNull
      - updated_at: timestamp with timezone, defaultNow, notNull
    - RLS policies:
      - SELECT: authenticated role, USING org_id matches jwt org_id
      - No INSERT/UPDATE/DELETE for authenticated role (webhook handler uses adminDb)
    - Enable RLS

    Update `src/lib/db/schema/index.ts`:
    - Add `export * from "./subscriptions"`

    Update `src/lib/db/schema/relations.ts`:
    - Add subscriptionRelations: one-to-one with orgs (subscription belongs to one org)
  </action>
  <verify>
    1. `npm run build` succeeds
    2. subscriptions.ts exports subscriptions table, subscriptionStatusEnum, billingTierEnum
    3. schema/index.ts re-exports subscriptions
    4. relations.ts includes subscription relation
  </verify>
  <done>
    Subscriptions schema exists with all required columns, enums, RLS policies, and relations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Stripe client module with tier configuration</name>
  <files>
    src/lib/stripe.ts
  </files>
  <action>
    Create `src/lib/stripe.ts`:
    - Import Stripe from "stripe"
    - Export `stripe` singleton: `new Stripe(process.env.STRIPE_SECRET_KEY!, { apiVersion: "2025-03-31.basil", typescript: true })`
    - Export `TIER_LIMITS` constant: `{ starter: 79, pro: 200, enterprise: Infinity } as const`
    - Export `TIER_PRICE_IDS` constant reading from env vars:
      ```
      {
        starter: { monthly: process.env.STRIPE_PRICE_STARTER_MONTHLY!, annual: process.env.STRIPE_PRICE_STARTER_ANNUAL! },
        pro: { monthly: process.env.STRIPE_PRICE_PRO_MONTHLY!, annual: process.env.STRIPE_PRICE_PRO_ANNUAL! },
        enterprise: { monthly: process.env.STRIPE_PRICE_ENTERPRISE_MONTHLY!, annual: process.env.STRIPE_PRICE_ENTERPRISE_ANNUAL! },
      }
      ```
    - Export types: `BillingTier`, `BillingInterval`
    - Export helper: `getTierForPoolCount(count: number): BillingTier` — returns the minimum tier that accommodates the given pool count
    - Export helper: `getPoolLimitForTier(tier: BillingTier): number` — returns the pool limit for a tier

    CRITICAL: The Stripe API version string must match what's actually available. Check Stripe docs for the latest stable version. If unsure, omit the apiVersion and let the SDK use its default.
  </action>
  <verify>
    1. `npm run build` succeeds
    2. stripe.ts exports stripe, TIER_LIMITS, TIER_PRICE_IDS, BillingTier, BillingInterval, getTierForPoolCount, getPoolLimitForTier
  </verify>
  <done>
    Stripe client singleton and tier configuration ready for use by checkout, webhook, and billing actions.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- subscriptions table schema compiles with correct column types and RLS
- Stripe client module exports all required utilities
- No circular imports between schema files
</verification>

<success_criteria>
The subscriptions data model and Stripe integration foundation exist — ready for checkout flow (14-02) and webhook handler (14-03) to build upon.
</success_criteria>

<output>
After completion, create `.planning/phases/12-subscription-billing/14-01-SUMMARY.md`
</output>
