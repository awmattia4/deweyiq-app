---
phase: 04-scheduling-routing
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/schedule/unassigned-panel.tsx
  - src/components/schedule/route-builder.tsx
  - src/components/schedule/copy-route-dialog.tsx
  - src/actions/schedule.ts
autonomous: true
requirements:
  - SCHED-01

must_haves:
  truths:
    - "Office sees an unassigned customers panel in the route builder"
    - "Office can drag a customer from the unassigned panel onto the stop list to assign them"
    - "Office can click to assign (not just drag) for accessibility"
    - "Office can multi-select customers in the unassigned panel for bulk assignment"
    - "Office can copy/duplicate an entire day's route to another day or tech"
  artifacts:
    - path: "src/components/schedule/unassigned-panel.tsx"
      provides: "Sidebar panel listing unassigned customers"
      contains: "UnassignedPanel"
    - path: "src/components/schedule/copy-route-dialog.tsx"
      provides: "Dialog for copying route to another day/tech"
      contains: "CopyRouteDialog"
  key_links:
    - from: "src/components/schedule/route-builder.tsx"
      to: "src/components/schedule/unassigned-panel.tsx"
      via: "renders UnassignedPanel inside DndContext"
      pattern: "UnassignedPanel"
    - from: "src/components/schedule/unassigned-panel.tsx"
      to: "src/actions/schedule.ts"
      via: "click-to-assign calls assignStopToRoute"
      pattern: "assignStopToRoute"
    - from: "src/components/schedule/copy-route-dialog.tsx"
      to: "src/actions/schedule.ts"
      via: "calls copyRoute server action"
      pattern: "copyRoute"
---

<objective>
Add the unassigned customer panel, multi-container drag-and-drop (unassigned to stop list), multi-select bulk assignment, and route copy/duplicate functionality to the route builder.

Purpose: SCHED-01 requires office to assign stops to techs. The unassigned panel is where unserviced customers live. Multi-select and copy-route are power-user features that make route building efficient for real-world pool companies with 50-200 customers.

Output: UnassignedPanel component, CopyRouteDialog, multi-container dnd-kit wiring in RouteBuilder, and bulk assignment server actions.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling-routing/04-RESEARCH.md
@.planning/phases/04-scheduling-routing/04-CONTEXT.md
@.planning/phases/04-scheduling-routing/04-01-SUMMARY.md
@.planning/phases/04-scheduling-routing/04-03-SUMMARY.md
@src/components/schedule/route-builder.tsx
@src/actions/schedule.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build unassigned panel with multi-select and click-to-assign</name>
  <files>
    src/components/schedule/unassigned-panel.tsx
    src/actions/schedule.ts
  </files>
  <action>
    **1. Create `src/components/schedule/unassigned-panel.tsx`:**

    "use client" component showing customers not yet assigned to the selected tech's route for the selected day.

    Props:
    - `customers: UnassignedCustomer[]` -- customers without a route_stop for this tech+day
    - `selectedIds: Set<string>` -- currently multi-selected customer IDs
    - `onToggleSelect: (customerId: string) => void`
    - `onAssign: (customerIds: string[]) => void` -- assign selected customers
    - `isOver?: boolean` -- whether a drag is hovering (for visual feedback)

    Each row shows:
    - Checkbox for multi-select
    - Customer name
    - Address (truncated)
    - Pool count badge
    - "Assign" button (single customer click-to-assign)

    Multi-select behavior per user decision:
    - Checkboxes allow selecting multiple customers
    - "Assign Selected (N)" button at the bottom when selection > 0
    - Clicking "Assign Selected" calls onAssign with all selected customer IDs
    - After assignment, selected customers disappear from the unassigned list

    Search/filter:
    - Text input at the top for filtering unassigned customers by name or address
    - Filter is client-side (the list is already loaded)

    Layout:
    - Scrollable panel (max-height with overflow-y-auto)
    - Appears as a right sidebar or collapsible drawer in the route builder
    - Per user decision: "shown in a sidebar panel"

    Define `UnassignedCustomer` type:
    ```ts
    interface UnassignedCustomer {
      id: string
      name: string
      address: string | null
      poolCount: number
      pools: Array<{ id: string; name: string }>
    }
    ```

    **2. Add server actions to `src/actions/schedule.ts`:**

    - `getUnassignedCustomers(techId: string, date: string)`:
      Fetch all customers in the org that do NOT have a route_stop for this tech+date.
      Query approach: fetch all org customers, fetch route_stop customer_ids for tech+date, filter out assigned ones in JS. (LEFT JOIN approach per RLS pitfall -- no correlated subquery.)
      Include pool count and pool names.

    - `bulkAssignStops(customerPoolPairs: Array<{customerId: string, poolId: string}>, techId: string, date: string)`:
      Create route_stop rows for each customer/pool pair. Assign sort_index starting after the last existing stop's sort_index.
      Owner/office only. Revalidate /schedule path.
  </action>
  <verify>
    - `npm run build` passes
    - UnassignedPanel component renders customer list with checkboxes
    - getUnassignedCustomers returns customers not on the current route
    - bulkAssignStops creates route_stop rows
  </verify>
  <done>
    Unassigned panel shows customers without route_stops for the selected tech+day. Multi-select checkboxes work. Click-to-assign and bulk "Assign Selected" both create route_stop rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire multi-container DnD and add copy-route dialog to route builder</name>
  <files>
    src/components/schedule/route-builder.tsx
    src/components/schedule/copy-route-dialog.tsx
    src/actions/schedule.ts
  </files>
  <action>
    **1. Update `src/components/schedule/route-builder.tsx` for multi-container DnD:**

    Wrap both UnassignedPanel and RouteStopList in a SINGLE DndContext per the RESEARCH.md Pattern 2 (multi-container dnd-kit):

    ```
    <DndContext onDragStart onDragOver onDragEnd>
      <UnassignedPanel /> (SortableContext with unassigned customer IDs)
      <RouteStopList />   (SortableContext with route stop IDs)
      <DragOverlay />     (ghost of dragged item)
    </DndContext>
    ```

    Key behaviors:
    - Drag FROM unassigned panel TO stop list: creates a new route_stop at the drop position
    - Drag WITHIN stop list: reorder (already working from Plan 04-03)
    - Cannot drag FROM stop list TO unassigned (use "Remove" button instead)

    onDragOver handler:
    - Detect cross-container move (item from unassigned hovering over stop list)
    - Add the customer to the stop list temporarily at the hover position for visual feedback
    - Remove from unassigned list temporarily

    onDragEnd handler:
    - If cross-container: call assignStopToRoute server action to persist
    - If same-container reorder: call updateStopOrder server action
    - If drag cancelled (no over): revert temporary changes

    DragOverlay:
    - Renders a ghost card of the dragged item (customer card from unassigned or stop row from list)
    - Uses a shared `DragGhost` component that renders differently based on item type

    Container detection:
    - Use `data` attribute on SortableContext items: `{ container: "unassigned" }` vs `{ container: "stops" }`
    - Or detect by checking if active.id is in unassigned list vs stops list

    **2. Add the UnassignedPanel to the route builder layout:**

    Update the layout to include the unassigned panel:
    ```
    +-------------------------------------------------------+
    | [Tech tabs] [Day picker]                              |
    +----------------+--------------------+-----------------+
    | Unassigned     | Stop List          | Route Map       |
    | Panel          | (sortable)         | (MapLibre)      |
    | [search]       | 1. Customer A      | [map with       |
    | [ ] Cust X     | 2. Customer B      |  pins and       |
    | [ ] Cust Y     | 3. Customer C      |  route line]    |
    | [Assign (2)]   |                    |                 |
    +----------------+--------------------+-----------------+
    ```

    Responsive: On mobile, unassigned panel is a collapsible drawer/sheet. On desktop, it's a fixed-width left sidebar (about 250px).

    Fetch unassigned customers when tech+day changes (alongside stops).

    **3. Create `src/components/schedule/copy-route-dialog.tsx`:**

    "use client" dialog for copying/duplicating an entire day's route.

    Props:
    - `sourceTechId: string`
    - `sourceDate: string`
    - `techs: Array<{ id: string; name: string }>`
    - `onCopyComplete: () => void`

    Dialog content:
    - "Copy Route" title
    - Source info: "Copying [Tech Name]'s [Day] route (N stops)"
    - Target tech selector (dropdown of org techs)
    - Target day selector (Mon-Fri buttons + date picker for choosing a specific week)
    - "Copy" button calls copyRoute server action
    - Success toast via sonner on completion

    **4. Add copyRoute server action to `src/actions/schedule.ts`:**

    - `copyRoute(sourceTechId: string, sourceDate: string, targetTechId: string, targetDate: string)`:
      1. Fetch all route_stops for sourceTech+sourceDate
      2. Create new route_stops for targetTech+targetDate with same customer_id, pool_id, sort_index, position_locked, window_start, window_end
      3. Skip if target already has stops for those customers (upsert with onConflictDoNothing)
      4. Owner/office only
      5. Return count of copied stops

    **5. Add "Copy Route" button to route builder:**

    Add a "Copy Route" button in the route builder toolbar (next to tech tabs or in a menu). Opens CopyRouteDialog with current tech+day as source.
  </action>
  <verify>
    - `npm run build` passes
    - Dragging from unassigned panel to stop list creates a route_stop (check build compiles; runtime tested in Plan 04-07)
    - CopyRouteDialog renders with tech and day selectors
    - copyRoute server action exists and creates stop rows
  </verify>
  <done>
    Route builder has multi-container DnD (drag from unassigned panel to stop list). Unassigned panel integrated with search, multi-select, and click-to-assign. Copy Route dialog lets office duplicate a day's route to another day or tech.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero TypeScript errors
- Unassigned panel shows customers not on the current route
- Multi-select and bulk assign work
- Drag from unassigned to stop list creates route_stops
- Copy route dialog copies stops from one tech+day to another
- DragOverlay shows ghost of dragged item
</verification>

<success_criteria>
Office staff can see unassigned customers in a sidebar panel, drag them onto the stop list or click to assign, multi-select for bulk assignment, and copy an entire day's route to another day or tech. The route builder feels like a project management tool per user's specific idea.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-routing/04-04-SUMMARY.md`
</output>
