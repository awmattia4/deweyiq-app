---
phase: 04-scheduling-routing
plan: "07"
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
  - "04-06"
files_modified: []
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
must_haves:
  truths:
    - "All SCHED requirements verified by human testing the full scheduling workflow"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 4 scheduling and routing system. Office staff tests the full workflow: building routes, setting schedules, optimizing, and viewing dispatch.

Purpose: Phase 4 is a complex multi-surface phase (route builder, dispatch map, schedule generation, optimization). Human verification ensures all components work together and the UX meets the user's vision of a "project management tool" feel.

Output: Verified or issues documented for gap closure.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling-routing/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of complete Phase 4 Scheduling and Routing system</name>
  <files></files>
  <action>
    Present the following to the user for verification:

    **What was built:**

    Complete Phase 4 Scheduling and Routing system:

    1. **Route Builder** (/schedule -> Routes tab):
       - Split view: stop list on left, map with plotted route on right
       - Tech tabs across top to switch between techs
       - Day-of-week picker (Mon-Fri)
       - Drag-and-drop reorder stops within the list
       - Map updates instantly when stops are reordered
       - Unassigned customers panel with drag-to-assign and click-to-assign
       - Multi-select in unassigned panel for bulk assignment
       - Copy/duplicate route to another day or tech
       - Lock stops to fixed positions (lock icon)

    2. **Recurring Schedules** (/schedule -> Rules tab):
       - Create schedule rules (weekly, bi-weekly, monthly, custom frequency)
       - Edit and deactivate rules
       - Auto-generation of route_stops for the next 4 weeks
       - Frequency change deletes all future stops and regenerates

    3. **Holiday Calendar** (/schedule -> Holidays tab):
       - Add/remove company holidays
       - Auto-generated stops skip holiday dates

    4. **Route Optimization** (Optimize Route button in route builder):
       - One-click optimization via OpenRouteService
       - Locked stops stay in position
       - Before/after preview with drive time comparison
       - Apply or Cancel

    5. **Live Dispatch Map** (/dispatch):
       - MapLibre map showing tech positions as colored pulsing pins
       - Route lines through remaining stops per tech
       - Completed stops grayed out
       - Click stop marker -> popup card with customer info and profile link
       - All-techs view (default) and single-tech filter
       - GPS broadcast from tech app (when /routes page is open)

    6. **Tech App Compatibility**:
       - /api/routes/today reads from route_stops (with route_days fallback)
       - Tech route view unchanged functionally

    **How to verify:**

    Prerequisite: Ensure at least 2 techs and 5+ customers with addresses exist in the system. Set NEXT_PUBLIC_MAPTILER_KEY and ORS_API_KEY env vars.

    Test 1 - Route Builder (SCHED-01, SCHED-04):
    1. Log in as owner/office
    2. Navigate to /schedule (should see the Routes tab by default)
    3. Select a tech tab -> should see their stops for the current day (or empty if new)
    4. Switch between days (Mon-Fri) -> stops reload per day
    5. Open the unassigned panel -> should see customers without stops for this tech+day
    6. Drag a customer from unassigned to the stop list at a specific position -> stop created
    7. Click "Assign" on another customer -> stop added to end of list
    8. Multi-select 2 customers with checkboxes -> click "Assign Selected" -> both added
    9. Drag stops in the list to reorder -> map route line updates instantly
    10. Click lock icon on a stop -> stop shows locked state, drag handle hidden
    11. Click "Copy Route" -> select another day/tech -> stops duplicated

    Test 2 - Recurring Schedules (SCHED-02, SCHED-03):
    1. Go to /schedule -> Rules tab
    2. Click "Add Rule" -> fill in customer, tech, weekly frequency, anchor date
    3. Save -> rule appears in list AND route_stops appear in the Routes tab for the next 4 weeks
    4. Edit the rule -> change frequency to bi-weekly -> future stops regenerated (fewer stops visible)
    5. Deactivate the rule -> future stops deleted

    Test 3 - Holidays (SCHED-02):
    1. Go to /schedule -> Holidays tab
    2. Add a holiday for an upcoming date
    3. Check Routes tab for that date -> stops should show "holiday" status or be absent

    Test 4 - Route Optimization (SCHED-05):
    1. Go to Routes tab with 3+ stops on a day (at least 2 unlocked)
    2. Lock one stop
    3. Click "Optimize Route" -> wait for ORS response
    4. Before/after preview appears with two columns and drive time comparison
    5. Verify locked stop is in same position in both columns
    6. Click "Apply" -> route order updates, toast shows time saved
    7. Try again and click "Cancel" -> order unchanged

    Test 5 - Live Dispatch (SCHED-06):
    1. Open /dispatch in a browser tab (as owner/office)
    2. In another browser/device, log in as tech and open /routes
    3. Tech's position should appear as a colored pin on the dispatch map (may take a few seconds)
    4. Stops for today should appear as numbered markers with route line
    5. Click a stop marker -> popup card shows customer info
    6. Click the customer profile link in the popup -> navigates to /customers/[id]
    7. Use tech filter -> single tech view shows only that tech's stops
    8. Complete a stop in the tech app -> dispatch map shows it grayed out (may need refresh)

    Test 6 - Tech App Backward Compatibility:
    1. Log in as tech -> /routes should show today's stops (same as Phase 3)
    2. Complete a stop -> works as before
    3. No visible changes to the tech experience

    Resume signal: Type "approved" to complete Phase 4, or describe any issues found.
  </action>
  <verify>Human has tested all 6 SCHED requirements end-to-end and confirmed the system works.</verify>
  <done>All SCHED-01 through SCHED-06 requirements verified by human testing. Phase 4 complete.</done>
</task>

</tasks>

<verification>
Human has tested all 6 SCHED requirements end-to-end and confirmed the system works.
</verification>

<success_criteria>
All SCHED-01 through SCHED-06 requirements verified by human testing. Route builder, recurring schedules, holidays, optimization, and dispatch map all function correctly. Tech app backward compatibility confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-routing/04-07-SUMMARY.md`
</output>
