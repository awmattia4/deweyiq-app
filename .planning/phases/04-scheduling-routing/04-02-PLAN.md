---
phase: 04-scheduling-routing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/schedule/page.tsx
  - src/app/(app)/schedule/loading.tsx
  - src/app/(app)/dispatch/page.tsx
  - src/app/(app)/dispatch/loading.tsx
  - src/actions/schedule.ts
  - src/components/schedule/schedule-rule-dialog.tsx
  - src/components/schedule/holiday-calendar.tsx
  - supabase/functions/generate-schedule/index.ts
autonomous: true
requirements:
  - SCHED-02
  - SCHED-03

must_haves:
  truths:
    - "Office can create a recurring schedule rule for a customer/pool with frequency (weekly, biweekly, monthly, custom)"
    - "Office can edit and deactivate schedule rules"
    - "Office can manage a company holiday calendar (add/remove holidays)"
    - "The schedule generator creates route_stops rows for the next 4 weeks based on active rules, skipping holidays"
    - "On frequency change, future stops are deleted and regenerated from today"
  artifacts:
    - path: "src/actions/schedule.ts"
      provides: "Schedule rule CRUD + generation logic"
      contains: "createScheduleRule"
    - path: "src/components/schedule/schedule-rule-dialog.tsx"
      provides: "Dialog for creating/editing schedule rules"
      contains: "ScheduleRuleDialog"
    - path: "src/components/schedule/holiday-calendar.tsx"
      provides: "Holiday management UI"
      contains: "HolidayCalendar"
    - path: "supabase/functions/generate-schedule/index.ts"
      provides: "Edge Function for rolling 4-week generation"
      contains: "Deno.serve"
  key_links:
    - from: "src/actions/schedule.ts"
      to: "src/lib/db/schema/schedule-rules.ts"
      via: "import scheduleRules"
      pattern: "from.*schedule-rules"
    - from: "supabase/functions/generate-schedule/index.ts"
      to: "schedule_rules table"
      via: "Supabase client query"
      pattern: "from.*schedule_rules"
    - from: "src/components/schedule/schedule-rule-dialog.tsx"
      to: "src/actions/schedule.ts"
      via: "server action call"
      pattern: "createScheduleRule|updateScheduleRule"

user_setup:
  - service: supabase-cron
    why: "Schedule generation requires pg_cron to trigger Edge Function weekly"
    dashboard_config:
      - task: "Enable pg_cron extension and create weekly cron job"
        location: "Supabase Dashboard -> Database -> Extensions -> pg_cron; then SQL Editor to create cron.schedule()"
---

<objective>
Build the recurring schedule system -- office can set service frequencies per customer/pool, manage a company holiday calendar, and a Supabase Edge Function auto-generates route_stops for the rolling 4-week window.

Purpose: SCHED-02 and SCHED-03 require the system to auto-generate future stops from schedule rules without manual entry. This eliminates daily manual route creation and is the backbone of the scheduling workflow.

Output: Schedule rule CRUD actions, holiday calendar management UI, schedule rule dialog, Edge Function for rolling generation, and placeholder pages for /schedule and /dispatch routes.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling-routing/04-RESEARCH.md
@.planning/phases/04-scheduling-routing/04-CONTEXT.md
@src/lib/db/schema/route-stops.ts
@src/lib/db/schema/schedule-rules.ts
@src/lib/db/schema/holidays.ts
@src/lib/db/schema/customers.ts
@src/actions/schedule.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build schedule rule CRUD, holiday management, and generation logic</name>
  <files>
    src/actions/schedule.ts
    src/components/schedule/schedule-rule-dialog.tsx
    src/components/schedule/holiday-calendar.tsx
  </files>
  <action>
    **1. Extend `src/actions/schedule.ts` with schedule rule actions:**

    Note: Plan 04-01 creates the initial schedule.ts with migration and route_stop CRUD. This task adds schedule-rule-specific actions. If 04-01 and 04-02 run in parallel (same wave), this task creates schedule.ts from scratch including both route_stop CRUD and schedule rule actions. Check if the file exists first; if it does, extend it.

    Add these server actions:

    - `getScheduleRules(techId?: string)`: Fetch all active schedule_rules for the org. Optionally filter by tech. JOIN with customers and pools to include names. Owner/office only.

    - `createScheduleRule(data: { customerId, poolId?, techId?, frequency, customIntervalDays?, anchorDate, preferredDayOfWeek? })`:
      INSERT into schedule_rules. Owner/office only (via getCurrentUser role check).
      After creating the rule, call `generateStopsForRule(ruleId)` to immediately generate the first 4 weeks of stops.

    - `updateScheduleRule(ruleId: string, data: Partial<...>)`:
      UPDATE the schedule_rule. Owner/office only.
      If frequency changed: delete all future route_stops for this rule (WHERE schedule_rule_id = ruleId AND scheduled_date > today) and regenerate from today per user decision (destructive regeneration on frequency change).

    - `deleteScheduleRule(ruleId: string)`:
      Set active = false (soft delete). Delete future route_stops for this rule.

    - `generateStopsForRule(ruleId: string)`:
      Internal helper (not exported as server action). Reads the schedule_rule, computes dates for the next 28 days from today, checks org holidays, and upserts route_stops rows. Uses the date generation algorithm from RESEARCH.md:
      - For weekly: every 7 days from anchor
      - For biweekly: every 14 days from anchor
      - For monthly: same day-of-month each month (handle month-end edge cases)
      - For custom: every N days from anchor
      - Skip dates that fall on org holidays
      - Upsert with onConflictDoNothing on (org_id, customer_id, pool_id, scheduled_date) unique constraint
      - New stops get sort_index = 999 (appended to end; office reorders via route builder)

    - `generateAllScheduleStops()`:
      Iterates all active schedule_rules for the org and calls generateStopsForRule for each. This is the function the Edge Function will call.

    **Holiday management actions:**

    - `getHolidays(year?: number)`: Fetch holidays for the org, optionally filtered by year.

    - `createHoliday(data: { date: string, name: string })`:
      INSERT into holidays. Owner/office only.
      After creating, check if any route_stops exist on this date -- if so, update their status to 'holiday'.

    - `deleteHoliday(holidayId: string)`:
      DELETE the holiday. Owner/office only.
      Reset any route_stops on this date from 'holiday' status back to 'scheduled'.

    **2. Create `src/components/schedule/schedule-rule-dialog.tsx`:**

    A client component dialog (using shadcn Dialog) for creating/editing schedule rules:
    - Customer selector (dropdown of org customers)
    - Pool selector (dropdown of selected customer's pools, optional)
    - Tech selector (dropdown of org techs)
    - Frequency selector: Weekly, Bi-weekly, Monthly, Custom
    - If Custom: number input for interval days
    - Anchor date picker (date input)
    - Preferred day of week selector (Mon-Fri dropdown, optional)
    - Submit calls createScheduleRule or updateScheduleRule server action

    Use plain React state + inline validation (matches project pattern from Phase 2 -- NO zod/hookform per known incompatibility).
    On success, show toast via sonner and close dialog.

    **3. Create `src/components/schedule/holiday-calendar.tsx`:**

    A client component for managing the company holiday calendar:
    - List of holidays for the current year (expandable to other years)
    - "Add Holiday" button opens a small inline form (date picker + name text input)
    - Each holiday row has a delete button (with confirmation)
    - Pre-populated suggestion list for US holidays (user picks which to add)
    - Calls createHoliday/deleteHoliday server actions
    - Intended to be rendered in settings or as a section of the schedule page
  </action>
  <verify>
    - `npm run build` passes
    - Server actions import correctly from schedule-rules and holidays schema
    - ScheduleRuleDialog renders without errors (check build, not runtime)
    - HolidayCalendar renders without errors
  </verify>
  <done>
    Schedule rule CRUD actions work (create, update, delete with future stop regeneration). Holiday CRUD actions work (create, delete with route_stop status updates). ScheduleRuleDialog provides a form for creating/editing rules. HolidayCalendar provides holiday management UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Edge Function for rolling schedule generation and placeholder pages</name>
  <files>
    supabase/functions/generate-schedule/index.ts
    src/app/(app)/schedule/page.tsx
    src/app/(app)/schedule/loading.tsx
    src/app/(app)/dispatch/page.tsx
    src/app/(app)/dispatch/loading.tsx
  </files>
  <action>
    **1. Create `supabase/functions/generate-schedule/index.ts`:**

    A Supabase Edge Function (Deno runtime) that generates the rolling 4-week schedule:

    ```
    import { createClient } from "https://esm.sh/@supabase/supabase-js@2"

    Deno.serve(async (req) => {
      // Use service role key (bypasses RLS) for cross-org generation
      const supabase = createClient(
        Deno.env.get("SUPABASE_URL")!,
        Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
      )

      const today = new Date()
      const windowEnd = new Date(today)
      windowEnd.setDate(today.getDate() + 28)

      // Fetch all active schedule rules across all orgs
      const { data: rules } = await supabase
        .from("schedule_rules")
        .select("*")
        .eq("active", true)

      // Fetch holidays within the generation window
      const todayStr = today.toISOString().split("T")[0]
      const windowEndStr = windowEnd.toISOString().split("T")[0]
      const { data: holidays } = await supabase
        .from("holidays")
        .select("date, org_id")
        .gte("date", todayStr)
        .lte("date", windowEndStr)

      const holidaySet = new Set(
        holidays?.map(h => `${h.org_id}:${h.date}`) ?? []
      )

      let totalGenerated = 0

      for (const rule of rules ?? []) {
        const dates = generateDatesForRule(rule, today, windowEnd)
        for (const date of dates) {
          const dateStr = date.toISOString().split("T")[0]
          if (holidaySet.has(`${rule.org_id}:${dateStr}`)) continue

          const { error } = await supabase.from("route_stops").upsert({
            org_id: rule.org_id,
            customer_id: rule.customer_id,
            pool_id: rule.pool_id,
            tech_id: rule.tech_id,
            schedule_rule_id: rule.id,
            scheduled_date: dateStr,
            sort_index: 999,
            status: "scheduled",
          }, { onConflict: "org_id,customer_id,pool_id,scheduled_date", ignoreDuplicates: true })

          if (!error) totalGenerated++
        }
      }

      return new Response(JSON.stringify({ generated: totalGenerated }), {
        headers: { "Content-Type": "application/json" },
      })
    })
    ```

    The `generateDatesForRule` function computes all service dates within the window based on frequency:
    - weekly: every 7 days from anchor
    - biweekly: every 14 days from anchor
    - monthly: same day each month (use anchor day-of-month; clamp to month-end if needed)
    - custom: every custom_interval_days from anchor

    IMPORTANT: This file is Deno, NOT Node.js. Use Deno-compatible imports (esm.sh). Do NOT import from @/ aliases or Node.js modules. Ensure it's excluded from the main tsconfig.json (already excluded per Phase 3 decision -- verify supabase/functions is in tsconfig exclude).

    **2. Create placeholder schedule page `src/app/(app)/schedule/page.tsx`:**

    Server component that:
    - Authenticates via getCurrentUser(), redirects non-owner/office to /routes
    - Fetches schedule rules via getScheduleRules()
    - Renders a simple layout with:
      - Title "Schedule" with "Add Rule" button that opens ScheduleRuleDialog
      - A table/list of existing schedule rules showing: Customer name, Pool name, Tech name, Frequency, Next service date, Active status
      - HolidayCalendar component in a collapsible section or tab
    - This is the placeholder for the full route builder (Plan 04-03 builds the real split-view)

    **3. Create `src/app/(app)/schedule/loading.tsx`:**
    Skeleton with table rows matching the schedule rule list layout.

    **4. Create placeholder dispatch page `src/app/(app)/dispatch/page.tsx`:**

    Server component that:
    - Authenticates, redirects non-owner/office to /routes
    - Shows a "Dispatch Map" heading with a placeholder div for the map (Plan 04-05 builds the real map)
    - Shows text: "Live dispatch map coming soon"

    **5. Create `src/app/(app)/dispatch/loading.tsx`:**
    Skeleton with a large map placeholder rectangle and sidebar skeleton.
  </action>
  <verify>
    - `npm run build` passes (Edge Function excluded from Node.js build)
    - `/schedule` page renders for owner/office (check by navigating to it)
    - `/dispatch` page renders for owner/office
    - Edge Function file exists at `supabase/functions/generate-schedule/index.ts` and uses Deno-compatible imports
  </verify>
  <done>
    Edge Function generates route_stops for the next 4 weeks based on active schedule rules, skipping holidays. /schedule page shows schedule rules list with add/edit dialog and holiday calendar. /dispatch page has placeholder for the live map. Loading skeletons exist for both pages.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero TypeScript errors
- Schedule rule CRUD works: create/update/delete with automatic future stop generation
- Holiday management works: add/remove holidays, route_stops on holiday dates update status
- Edge Function generates correct dates for weekly, biweekly, monthly, and custom frequencies
- /schedule and /dispatch pages render for owner/office roles
- Edge Function uses Deno imports (not Node.js)
</verification>

<success_criteria>
Office staff can create recurring schedule rules for customers with any frequency. The system auto-generates route_stops for the next 4 weeks. Holidays are managed via a calendar UI. Frequency changes regenerate all future stops. The Edge Function can be triggered by pg_cron for ongoing generation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-routing/04-02-SUMMARY.md`
</output>
