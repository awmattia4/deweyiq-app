---
phase: 10-smart-features-ai
plan: 10
type: execute
wave: 2
depends_on: ["10-09"]
files_modified:
  - src/lib/notifications/default-templates.ts
  - src/actions/notifications.ts
  - src/actions/invoices.ts
  - src/actions/payments.ts
  - src/actions/quotes.ts
  - src/actions/work-orders.ts
  - src/actions/visits.ts
  - src/actions/routes.ts
  - src/actions/billing.ts
  - src/actions/schedule.ts
autonomous: true
requirements:
  # Company-facing event triggers (NOTIF-05 through NOTIF-22):
  - NOTIF-05   # stop_completed -> notifyOrgRole('owner+office')
  - NOTIF-06   # stop_skipped -> notifyOrgRole('owner+office')
  - NOTIF-07   # stop_cant_complete -> notifyOrgRole('owner+office')
  - NOTIF-08   # route_started/route_finished -> notifyOrgRole('owner+office')
  - NOTIF-09   # chemistry_alert -> notifyOrgRole('owner+office')
  - NOTIF-10   # wo_created/updated/completed -> notifyOrgRole('owner+office')
  - NOTIF-11   # quote_approved/rejected -> notifyOrgRole('owner+office')
  - NOTIF-12   # payment_received -> notifyOrgRole('owner+office')
  - NOTIF-13   # payment_failed -> notifyOrgRole('owner+office')
  - NOTIF-14   # portal_message -> notifyOrgRole('owner+office')
  - NOTIF-15   # service_request -> notifyOrgRole('owner+office')
  - NOTIF-16   # customer_added/cancelled -> notifyOrgRole('owner+office')
  - NOTIF-17   # invoice_overdue -> notifyOrgRole('owner+office')
  - NOTIF-18   # weather_proposal -> notifyOrgRole('owner+office')
  - NOTIF-19   # tech_assigned -> notifyUser(techId)
  - NOTIF-20   # tech_quote_approved -> notifyUser(techId)
  - NOTIF-21   # schedule_change -> notifyUser(techId)
  - NOTIF-22   # tech_weather_alert -> notifyUser(techId)
  # Customer-facing dual-channel (NOTIF-24 through NOTIF-32):
  - NOTIF-24   # pre-arrival email + SMS (migrate existing Edge Function to template system)
  - NOTIF-25   # service report email + SMS
  - NOTIF-26   # invoice email + SMS
  - NOTIF-27   # payment receipt email + SMS
  - NOTIF-28   # payment failure email + SMS
  - NOTIF-29   # quote ready email + SMS
  - NOTIF-30   # weather delay email + SMS
  - NOTIF-31   # WO status email + SMS
  - NOTIF-32   # portal reply email + SMS

must_haves:
  truths:
    - "Every customer-facing event sends both email AND SMS"
    - "Pre-arrival, service report, invoice, payment receipt/failure, quote, weather delay, WO status, and portal message replies all trigger dual-channel notifications"
    - "New template types are added to the customizable template system"
    - "Every company-facing event (stop complete/skip/cant-complete, route start/finish, WO, quote, payment, portal, weather, schedule) triggers in-app + push notification via notifyOrgRole/notifyUser"
    - "Existing pre-arrival notification in src/actions/notifications.ts is migrated to use getResolvedTemplate()"
  artifacts:
    - path: "src/lib/notifications/default-templates.ts"
      provides: "Default templates for all customer notification types"
    - path: "src/actions/notifications.ts"
      provides: "Enhanced notification dispatch with all customer touchpoints, migrated pre-arrival using template system"
  key_links:
    - from: "src/actions/invoices.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on invoice send"
      pattern: "notifyUser|notifyOrgRole"
    - from: "src/actions/payments.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on payment success/failure"
      pattern: "notifyUser|notifyOrgRole"
    - from: "src/actions/work-orders.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on WO status changes"
      pattern: "notifyUser|notifyOrgRole"
    - from: "src/actions/visits.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on stop completion/skip/cant-complete"
      pattern: "notifyOrgRole"
    - from: "src/actions/notifications.ts"
      to: "src/lib/notifications/default-templates.ts"
      via: "pre-arrival uses getResolvedTemplate instead of hardcoded content"
      pattern: "getResolvedTemplate"
---

<objective>
Comprehensive notifications -- wire every company-facing and customer-facing event to the notification dispatch, plus migrate existing notifications to the customizable template system.

Purpose: Every customer-facing event should trigger both email and SMS notifications. Every company-facing event should trigger in-app + push notifications for the relevant roles. This plan adds new notification template types, wires the unified dispatch into ALL server actions, and migrates the existing pre-arrival notification (Phase 5 Edge Function) into the customizable template system. This is the plan that actually satisfies NOTIF-05 through NOTIF-22 (company events) and NOTIF-24 through NOTIF-32 (customer dual-channel) by inserting `notifyOrgRole()` / `notifyUser()` calls into every relevant server action.

Output: Complete customer notification templates, wired dispatch in ALL relevant server actions, migrated pre-arrival notification.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-features-ai/10-09-SUMMARY.md
@src/lib/notifications/default-templates.ts
@src/actions/notifications.ts
@src/actions/invoices.ts
@src/actions/payments.ts
@src/actions/quotes.ts
@src/actions/work-orders.ts
@src/actions/visits.ts
@src/actions/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all customer notification templates and migrate pre-arrival to template system</name>
  <files>src/lib/notifications/default-templates.ts, src/actions/notifications.ts</files>
  <action>
    **Extend templates** — Add to `src/lib/notifications/default-templates.ts` ALL customer notification types (verify and add only what's missing):

    1. `pre_arrival_email` (may exist) — pre-arrival notification email
    2. `pre_arrival_sms` (may exist) — "{{company_name}}: Your pool tech {{tech_name}} is heading your way. Estimated arrival: {{eta_time}}."
    3. `service_report_email` (may exist) — service completion report with link
    4. `service_report_sms` (NEW) — "Your pool was serviced today. View report: {{report_link}}"
    5. `invoice_sms` (may exist) — "New invoice #{{invoice_number}} for {{amount}}. Pay online: {{pay_link}}"
    6. `payment_receipt_sms` (NEW) — "Payment of {{amount}} received. Thank you! -- {{company_name}}"
    7. `payment_failure_email` (may exist as dunning) — verify coverage
    8. `payment_failure_sms` (NEW) — "Payment of {{amount}} failed. Update your payment method: {{portal_link}} -- {{company_name}}"
    9. `wo_status_email` (NEW) — "Work order update: {{wo_title}} is now {{status}}"
    10. `wo_status_sms` (NEW) — "{{company_name}}: Your work order '{{wo_title}}' status: {{status}}"
    11. `portal_reply_email` (NEW) — "New reply to your message from {{company_name}}"
    12. `portal_reply_sms` (NEW) — "{{company_name}}: New reply to your message. View: {{portal_link}}"
    13. `weather_delay_email` (NEW) — "Service schedule change due to weather: {{weather_reason}}"
    14. `weather_delay_sms` (NEW) — "{{company_name}}: Your {{original_date}} service moved to {{new_date}} due to {{weather_reason}}."

    For each template: add to DEFAULT_TEMPLATES, ALL_TEMPLATE_TYPES, and TEMPLATE_TYPE_META.

    **Migrate pre-arrival (NOTIF-24)** — Update `src/actions/notifications.ts`:
    - In `sendPreArrivalNotifications()`, replace hardcoded email/SMS content with `getResolvedTemplate('pre_arrival_email', mergeData)` and `getResolvedTemplate('pre_arrival_sms', mergeData)`.
    - This ensures the pre-arrival notification goes through the customizable template system — owners can customize the pre-arrival message in Settings > Notifications.
    - Keep the existing Edge Function invoke pattern for actual delivery — just replace the content source from hardcoded to template-resolved.
    - Merge tags for pre-arrival: {{customer_name}}, {{company_name}}, {{tech_name}}, {{eta_time}}, {{service_date}}.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify all template types are in ALL_TEMPLATE_TYPES.</verify>
  <done>All customer notification template types exist in the default template system. Pre-arrival notification migrated to use customizable templates via getResolvedTemplate().</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification dispatch into ALL server actions (company + customer events)</name>
  <files>src/actions/invoices.ts, src/actions/payments.ts, src/actions/quotes.ts, src/actions/work-orders.ts, src/actions/visits.ts, src/actions/routes.ts, src/actions/billing.ts, src/actions/schedule.ts, src/actions/notifications.ts</files>
  <action>
    Wire the unified notification dispatch (from Plan 10-09) into ALL server actions. For each, add a fire-and-forget notification call AFTER the primary action succeeds. Every notification call is wrapped in try/catch — notification failure MUST NEVER block the primary action. Use `void notifyOrgRole(...)` or `Promise.allSettled()` pattern.

    **Company-facing event triggers (in-app + push via notifyOrgRole/notifyUser):**

    1. **src/actions/visits.ts** — `completeStop()`:
       - NOTIF-05: `notifyOrgRole(orgId, 'owner+office', { type: 'stop_completed', urgency: 'informational', title: 'Stop completed', body: '{techName} completed {customerName}', link: '/customers/{customerId}' })`
       - NOTIF-25: Send service_report_sms to customer (alongside existing email).

    2. **src/actions/visits.ts** (or routes.ts) — `skipStop()`:
       - NOTIF-06: `notifyOrgRole(orgId, 'owner+office', { type: 'stop_skipped', urgency: 'needs_action', title: 'Stop skipped', body: '{techName} skipped {customerName}: {reason}' })`

    3. **src/actions/visits.ts** (or routes.ts) — stop marked can't-complete:
       - NOTIF-07: `notifyOrgRole(orgId, 'owner+office', { type: 'stop_cant_complete', urgency: 'needs_action', title: 'Stop inaccessible', body: '{techName} could not service {customerName}: {reason}' })`

    4. **src/actions/notifications.ts** — `startRoute()`:
       - NOTIF-08: `notifyOrgRole(orgId, 'owner+office', { type: 'route_started', urgency: 'informational', title: 'Route started', body: '{techName} started their route ({stopCount} stops)' })`

    5. **src/actions/routes.ts** — route completion (all stops done):
       - NOTIF-08: `notifyOrgRole(orgId, 'owner+office', { type: 'route_finished', urgency: 'informational', title: 'Route finished', body: '{techName} completed their route' })`

    6. **src/actions/visits.ts** — when chemistry readings are out of range:
       - NOTIF-09: `notifyOrgRole(orgId, 'owner+office', { type: 'chemistry_alert', urgency: 'needs_action', title: 'Chemistry out of range', body: '{parameter} at {value} for {customerName}' })`

    7. **src/actions/work-orders.ts** — `createWorkOrder()`, `updateWorkOrderStatus()`:
       - NOTIF-10: `notifyOrgRole(orgId, 'owner+office', { type: 'wo_created'|'wo_updated'|'wo_completed', ... })`
       - NOTIF-19: When tech assigned: `notifyUser(assignedTechId, orgId, { type: 'tech_assigned', title: 'New work order assigned', body: '{woTitle} at {customerName}', link: '/work-orders/{woId}' })`
       - NOTIF-31: Send wo_status_sms to customer on status changes.

    8. **src/actions/quotes.ts** — quote approval/rejection:
       - NOTIF-11: `notifyOrgRole(orgId, 'owner+office', { type: 'quote_approved'|'quote_rejected', ... })`
       - NOTIF-20: `notifyUser(assignedTechId, orgId, { type: 'tech_quote_approved', ... })`
       - NOTIF-29: Send quote_ready SMS to customer.

    9. **src/actions/payments.ts** — `handlePaymentSucceeded()`:
       - NOTIF-12: `notifyOrgRole(orgId, 'owner+office', { type: 'payment_received', urgency: 'informational', ... })`
       - NOTIF-27: Send payment_receipt_sms to customer.

    10. **src/actions/payments.ts** — `handlePaymentFailed()`:
        - NOTIF-13: `notifyOrgRole(orgId, 'owner+office', { type: 'payment_failed', urgency: 'needs_action', ... })`
        - NOTIF-28: Send payment_failure_sms to customer.

    11. **Portal message handler** (in relevant portal action):
        - NOTIF-14: `notifyOrgRole(orgId, 'owner+office', { type: 'portal_message', urgency: 'needs_action', ... })`
        - NOTIF-32: Send portal_reply_sms when company replies to customer.

    12. **Portal service request handler**:
        - NOTIF-15: `notifyOrgRole(orgId, 'owner+office', { type: 'service_request', urgency: 'needs_action', ... })`

    13. **Customer CRUD** (in customers action or add-customer-dialog server action):
        - NOTIF-16: `notifyOrgRole(orgId, 'owner+office', { type: 'customer_added'|'customer_cancelled', ... })`

    14. **src/actions/billing.ts** or dunning cron — invoice overdue detection:
        - NOTIF-17: `notifyOrgRole(orgId, 'owner+office', { type: 'invoice_overdue', urgency: 'needs_action', ... })`

    15. **Weather proposal creation** (Plan 10-06 creates proposals — add notification here or verify it's called):
        - NOTIF-18: `notifyOrgRole(orgId, 'owner+office', { type: 'weather_proposal', urgency: 'needs_action', ... })`

    16. **src/actions/schedule.ts** — when stops are added/removed/reordered:
        - NOTIF-21: `notifyUser(techId, orgId, { type: 'schedule_change', ... })`

    17. **Weather alert integration** (Plan 10-07 adds weather badges — add tech notification):
        - NOTIF-22: `notifyUser(techId, orgId, { type: 'tech_weather_alert', urgency: 'needs_action', ... })`

    **CRITICAL**: For items 11-17, if the relevant server action doesn't exist yet or is in a different plan, add a `// TODO(10-10): Wire NOTIF-XX when [action] is available` comment at the appropriate location. The notification dispatch call should be added as soon as the triggering action exists. For weather (NOTIF-18, NOTIF-22) and schedule (NOTIF-21), these may need to be wired during execution of Plans 10-06, 10-07, and 10-03 respectively — add the dispatch calls in those server actions when they're modified.
  </action>
  <verify>Run `npm run build` — compiles without errors. Grep for `notifyOrgRole\|notifyUser` in src/actions/ to confirm all event triggers are wired.</verify>
  <done>All company-facing events trigger in-app + push notifications. All customer-facing events trigger dual-channel (email + SMS). Pre-arrival notification uses the customizable template system. Every NOTIF-05 through NOTIF-22 event type has a notifyOrgRole/notifyUser call in the relevant server action.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles successfully
- All customer notification template types are registered
- Server actions dispatch notifications after primary operations
- Notification failures don't block primary operations
- Pre-arrival notification uses getResolvedTemplate() instead of hardcoded content
- Grep `notifyOrgRole\|notifyUser` in src/actions/ shows wiring for all 18 NOTIF event types
</verification>

<success_criteria>
- Invoice, payment, quote, WO status, service report, weather delay, and portal reply events all send email + SMS to customers
- Stop complete/skip/cant-complete, route start/finish, WO, quote, payment, portal, weather, schedule events all trigger in-app + push for relevant roles
- Techs get notifications for assignments (NOTIF-19), quote approvals (NOTIF-20), schedule changes (NOTIF-21), and weather alerts (NOTIF-22)
- Pre-arrival notification migrated to customizable template system (NOTIF-24)
- All notifications use the customizable template system
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-10-SUMMARY.md`
</output>
