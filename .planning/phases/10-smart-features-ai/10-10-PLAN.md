---
phase: 10-smart-features-ai
plan: 10
type: execute
wave: 2
depends_on: ["10-09"]
files_modified:
  - src/lib/notifications/default-templates.ts
  - src/actions/notifications.ts
  - src/actions/invoices.ts
  - src/actions/payments.ts
  - src/actions/quotes.ts
  - src/actions/work-orders.ts
  - src/actions/visits.ts
autonomous: true
requirements:
  - NOTIF-24
  - NOTIF-25
  - NOTIF-26
  - NOTIF-27
  - NOTIF-28
  - NOTIF-29
  - NOTIF-30
  - NOTIF-31
  - NOTIF-32

must_haves:
  truths:
    - "Every customer-facing event sends both email AND SMS"
    - "Pre-arrival, service report, invoice, payment receipt/failure, quote, weather delay, WO status, and portal message replies all trigger dual-channel notifications"
    - "New template types are added to the customizable template system"
  artifacts:
    - path: "src/lib/notifications/default-templates.ts"
      provides: "Default templates for all customer notification types"
    - path: "src/actions/notifications.ts"
      provides: "Enhanced notification dispatch with all customer touchpoints"
  key_links:
    - from: "src/actions/invoices.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on invoice send"
      pattern: "notifyUser"
    - from: "src/actions/payments.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on payment success/failure"
      pattern: "notifyUser"
    - from: "src/actions/work-orders.ts"
      to: "src/lib/notifications/dispatch.ts"
      via: "notifies on WO status changes"
      pattern: "notifyUser"
---

<objective>
Comprehensive customer notifications -- email + SMS for every customer touchpoint.

Purpose: Every customer-facing event should trigger both email and SMS notifications. This plan adds new notification template types and wires the unified dispatch into all customer-facing server actions. Many of these notification sends already exist (e.g., pre-arrival via Edge Function, invoice email) -- this plan ensures EVERY touchpoint has dual-channel coverage and uses the customizable template system.

Output: Complete customer notification templates, wired dispatch in all relevant server actions.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-features-ai/10-09-SUMMARY.md
@src/lib/notifications/default-templates.ts
@src/actions/notifications.ts
@src/actions/invoices.ts
@src/actions/payments.ts
@src/actions/quotes.ts
@src/actions/work-orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all customer notification templates</name>
  <files>src/lib/notifications/default-templates.ts</files>
  <action>
    Extend `src/lib/notifications/default-templates.ts` to include ALL customer notification types. The following should be added (some may already exist -- verify and add only what's missing):

    1. `service_report_email` (may exist) — service completion report with link
    2. `service_report_sms` (NEW) — "Your pool was serviced today. View report: {{report_link}}"
    3. `invoice_sms` (may exist) — "New invoice #{{invoice_number}} for {{amount}}. Pay online: {{pay_link}}"
    4. `payment_receipt_sms` (NEW) — "Payment of {{amount}} received. Thank you! — {{company_name}}"
    5. `payment_failure_email` (may exist as dunning) — verify coverage
    6. `payment_failure_sms` (NEW) — "Payment of {{amount}} failed. Update your payment method: {{portal_link}} — {{company_name}}"
    7. `wo_status_email` (NEW) — "Work order update: {{wo_title}} is now {{status}}"
    8. `wo_status_sms` (NEW) — "{{company_name}}: Your work order '{{wo_title}}' status: {{status}}"
    9. `portal_reply_email` (NEW) — "New reply to your message from {{company_name}}"
    10. `portal_reply_sms` (NEW) — "{{company_name}}: New reply to your message. View: {{portal_link}}"

    For each template:
    - Add to `DEFAULT_TEMPLATES` object with default subject (email) and sms_text.
    - Add to `ALL_TEMPLATE_TYPES` array.
    - Add to `TEMPLATE_TYPE_META` with label and channel.
    - Include appropriate merge tags ({{customer_name}}, {{company_name}}, {{amount}}, {{link}}, etc.).

    Ensure total template count in TEMPLATE_TYPE_META comments stays accurate.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Verify all template types are in ALL_TEMPLATE_TYPES.</verify>
  <done>All customer notification template types exist in the default template system with appropriate merge tags.</done>
</task>

<task type="auto">
  <name>Task 2: Wire notification dispatch into customer-facing server actions</name>
  <files>src/actions/notifications.ts, src/actions/invoices.ts, src/actions/payments.ts, src/actions/quotes.ts, src/actions/work-orders.ts, src/actions/visits.ts</files>
  <action>
    Wire the unified notification dispatch (from Plan 10-09) into all customer-facing server actions. For each, add a fire-and-forget notification call AFTER the primary action succeeds:

    1. **src/actions/invoices.ts** — `sendInvoice()`:
       - After sending invoice email, also send SMS via `getResolvedTemplate('invoice_sms', ...)` + Edge Function invoke.
       - Also call `notifyOrgRole(orgId, 'owner+office', { type: 'invoice_sent', ... })` for in-app notification to office.

    2. **src/actions/payments.ts** — `handlePaymentSucceeded()` (webhook handler):
       - After recording payment, send receipt SMS via template.
       - Call `notifyOrgRole(orgId, 'owner+office', { type: 'payment_received', urgency: 'informational', ... })`.

    3. **src/actions/payments.ts** — `handlePaymentFailed()`:
       - Send payment failure SMS to customer.
       - Call `notifyOrgRole(orgId, 'owner+office', { type: 'payment_failed', urgency: 'needs_action', ... })`.

    4. **src/actions/quotes.ts** — quote approval/rejection handlers:
       - Already send email. Add SMS notification to customer on quote ready.
       - Call `notifyOrgRole()` for office on quote approved/rejected.
       - Call `notifyUser(assignedTechId, ...)` when tech's quote is approved (NOTIF-20).

    5. **src/actions/work-orders.ts** — `updateWorkOrderStatus()`:
       - On WO completion: send WO status SMS to customer.
       - Call `notifyOrgRole()` for office on WO created/updated/completed.
       - Call `notifyUser(assignedTechId, ...)` when tech is assigned a WO (NOTIF-19).

    6. **src/actions/visits.ts** (or routes.ts) — `completeStop()`:
       - After completion, send service_report_sms to customer (alongside existing email).
       - Call `notifyOrgRole('owner+office', { type: 'stop_completed', ... })`.

    7. **src/actions/notifications.ts** — `startRoute()`:
       - Call `notifyOrgRole('owner+office', { type: 'route_started', ... })`.

    For ALL notification sends: wrap in try/catch, log errors, never throw. Notification failure must NEVER block the primary action. Use `void notifyOrgRole(...)` or `Promise.allSettled()` pattern.
  </action>
  <verify>Run `npm run build` — compiles without errors.</verify>
  <done>All customer-facing events trigger dual-channel (email + SMS) notifications. All company-facing events trigger in-app + push notifications via unified dispatch.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles successfully
- All customer notification template types are registered
- Server actions dispatch notifications after primary operations
- Notification failures don't block primary operations
</verification>

<success_criteria>
- Invoice, payment, quote, WO status, service report, and portal reply events all send email + SMS to customers
- Office gets in-app + push notifications for all relevant events
- Techs get notifications for assignments and schedule changes
- All notifications use the customizable template system
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-10-SUMMARY.md`
</output>
