---
phase: 10-smart-features-ai
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/actions/alerts.ts
  - src/app/(app)/alerts/page.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements:
  - SMART-02

must_haves:
  truths:
    - "Pools with 6+ weeks of chemistry readings get predictive trend analysis"
    - "Office sees predictive alerts on the Alerts page and Dashboard for pools trending toward imbalance"
    - "Predictions show 'Early prediction' disclaimer when less than 3 months of data"
    - "R-squared threshold of 0.4 prevents false positive alerts from noisy data"
  artifacts:
    - path: "src/actions/alerts.ts"
      provides: "Predictive chemistry alert generation using OLS regression"
      exports: ["generateAlerts"]
    - path: "src/app/(app)/alerts/page.tsx"
      provides: "Alerts page showing predictive chemistry alerts"
  key_links:
    - from: "src/actions/alerts.ts"
      to: "src/lib/chemistry/prediction.ts"
      via: "computeLinearTrend on per-pool readings"
      pattern: "computeLinearTrend"
    - from: "src/actions/alerts.ts"
      to: "src/lib/chemistry/targets.ts"
      via: "target ranges for threshold comparison"
      pattern: "getTargetRanges"
---

<objective>
Predictive chemistry alerts using OLS regression on per-pool reading history.

Purpose: Office staff should see warnings about pools whose chemistry is trending toward imbalance BEFORE the problem occurs. This plan extends the existing alert generation system (`generateAlerts` in `alerts.ts`) to run OLS regression on each pool's last N readings and create predictive alerts when trends indicate upcoming imbalance. Per user decision: 6-week minimum data with disclaimer; full confidence after 3+ months. Predictive alerts visible to office + tech + customer (customer notification wired in Plan 10).

Output: Predictive chemistry alerts appearing on Alerts page and Dashboard.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-features-ai/10-RESEARCH.md
@.planning/phases/10-smart-features-ai/10-01-SUMMARY.md
@src/actions/alerts.ts
@src/lib/chemistry/prediction.ts
@src/lib/chemistry/targets.ts
@src/app/(app)/alerts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generateAlerts with predictive chemistry analysis</name>
  <files>src/actions/alerts.ts</files>
  <action>
    Extend the existing `generateAlerts()` function in `src/actions/alerts.ts`:

    1. After existing alert checks, add a new section: "Predictive Chemistry Alerts".

    2. Query service_visits for all pools in the org where there are 6+ visits with chemistry_readings (the MIN_VISITS_FOR_PREDICTION constant = 6 per user decision). Use adminDb. Group by pool_id. Order by service_date ascending within each pool.

    3. For each pool with sufficient history:
       - Extract each chemistry parameter (free_chlorine, ph, alkalinity, cya, calcium_hardness, salt) from the `chemistry_readings` JSONB.
       - Call `computeLinearTrend(values)` from `src/lib/chemistry/prediction.ts`.
       - Get target ranges from `getTargetRanges(sanitizerType)`.
       - If `slope < 0 AND projectedNext < target.min * 0.9 AND rSquared >= 0.4`, create a "predictive_low" alert: "pH trending low for [Customer] - [Pool]" (example).
       - If `slope > 0 AND projectedNext > target.max * 1.1 AND rSquared >= 0.4`, create a "predictive_high" alert.
       - Use alert_type = "predictive_chemistry" with reference_type = "pool" and reference_id = pool_id.
       - Store metadata JSONB: `{ parameter, slope, rSquared, projectedNext, visitCount, isEarlyPrediction: visitCount < 12 }`. 12 visits approximates 3 months of weekly service.

    4. Use `onConflictDoNothing` on the alerts unique constraint (org_id, alert_type, reference_id) to avoid duplicates — one active predictive alert per pool.

    5. Add severity: "info" for early predictions (< 12 visits), "warning" for confident predictions (>= 12 visits).
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors.</verify>
  <done>generateAlerts produces predictive chemistry alerts for pools with sufficient history, using OLS regression with R-squared confidence filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Display predictive alerts on Alerts page and Dashboard</name>
  <files>src/app/(app)/alerts/page.tsx, src/app/(app)/dashboard/page.tsx</files>
  <action>
    Update `src/app/(app)/alerts/page.tsx`:
    1. The alerts page already renders alerts from the DB. Ensure the "predictive_chemistry" alert_type renders with:
       - A trend icon (TrendingDown or TrendingUp from lucide-react) based on metadata.slope direction.
       - Show the parameter name, projected next value, and target range.
       - If `metadata.isEarlyPrediction === true`, show a disclaimer badge: "Early prediction -- accuracy improves with more data" in a muted info style.
       - Show the customer/pool name in the alert card (from the existing alert title).
    2. The alert card should be actionable — clicking navigates to `/customers/[customerId]` (extract from metadata or alert reference).

    Update `src/app/(app)/dashboard/page.tsx`:
    1. Add a "Predictive Alerts" section to the dashboard (below existing alerts or in a dedicated card).
    2. Query active predictive_chemistry alerts (dismissed_at IS NULL) — show count and the top 3 with parameter/pool name.
    3. Link to /alerts for full list.
    4. If no predictive alerts, don't show the section (no empty state needed — it's additive).
  </action>
  <verify>Run `npm run build` — pages compile without errors. Visit /alerts and /dashboard in dev to confirm rendering.</verify>
  <done>Predictive chemistry alerts appear on Alerts page with trend icons and early-prediction disclaimers. Dashboard shows a predictive alerts summary card.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles successfully
- Alerts page renders "predictive_chemistry" alerts with trend direction and confidence indicators
- Dashboard shows predictive alert count and top items
- No regressions to existing alert types (missed_stop, declining_chemistry, etc.)
</verification>

<success_criteria>
- Pools with 6+ visits get predictive analysis during alert generation
- R-squared >= 0.4 threshold prevents noisy false positives
- Early predictions (6-12 visits) show disclaimer
- Alerts appear on both Alerts page and Dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-02-SUMMARY.md`
</output>
