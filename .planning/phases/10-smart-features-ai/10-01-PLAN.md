---
phase: 10-smart-features-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chemistry/prediction.ts
  - src/lib/chemistry/__tests__/prediction.test.ts
  - src/lib/weather/open-meteo.ts
  - src/lib/chemistry/dosing.ts
autonomous: true
requirements:
  - SMART-01

must_haves:
  truths:
    - "Dosing recommendations factor in current weather temperature from Open-Meteo"
    - "Dosing recommendations factor in per-pool service history trend (last N visits)"
    - "AI-modified doses show a badge indicating weather or history adjustment"
  artifacts:
    - path: "src/lib/weather/open-meteo.ts"
      provides: "Open-Meteo API client with forecast fetch and weather classification"
      exports: ["fetchWeatherForecast", "classifyWeatherDay"]
    - path: "src/lib/chemistry/prediction.ts"
      provides: "OLS linear regression for chemistry trend analysis"
      exports: ["computeLinearTrend"]
    - path: "src/lib/chemistry/dosing.ts"
      provides: "Enhanced dosing with weather and history modifiers"
      exports: ["generateDosingRecommendations"]
  key_links:
    - from: "src/lib/chemistry/dosing.ts"
      to: "src/lib/weather/open-meteo.ts"
      via: "fetchWeatherForecast for temperature modifier"
      pattern: "fetchWeatherForecast"
    - from: "src/lib/chemistry/dosing.ts"
      to: "src/lib/chemistry/prediction.ts"
      via: "computeLinearTrend for history-aware adjustment"
      pattern: "computeLinearTrend"
---

<objective>
Enhanced smart chemical dosing with weather and history modifiers.

Purpose: The dosing engine currently uses only current readings and generic tables. This plan adds (1) an Open-Meteo API client for weather data, (2) a pure-TypeScript OLS linear regression function for chemistry trend analysis, and (3) weather + history modifiers to the existing dosing engine. Per user decision: AI as modifier badge on standard dose -- show rule-based dose as primary, when AI adjusts it, show a small badge with tap-for-details.

Output: Enhanced dosing engine, Open-Meteo client, OLS prediction function.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-features-ai/10-RESEARCH.md
@src/lib/chemistry/dosing.ts
@src/lib/chemistry/targets.ts
@src/lib/chemistry/__tests__/dosing.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open-Meteo client and OLS regression function</name>
  <files>src/lib/weather/open-meteo.ts, src/lib/chemistry/prediction.ts, src/lib/chemistry/__tests__/prediction.test.ts</files>
  <action>
    Create `src/lib/weather/open-meteo.ts`:
    - `fetchWeatherForecast(lat, lng, timezone?)` — fetches 7-day daily forecast from Open-Meteo REST API. Parameters: `daily=weather_code,precipitation_sum,precipitation_probability_max,wind_gusts_10m_max,temperature_2m_max`. Use `wind_speed_unit=mph`, `temperature_unit=fahrenheit`, `precipitation_unit=inch`. Cache with `next: { revalidate: 3600 }` (1 hour). Return typed `OpenMeteoForecast` interface or null on error.
    - `classifyWeatherDay(forecast, dayIndex)` — classifies a day as `clear | rain | storm | heat | wind` based on WMO codes and thresholds from research: thunderstorm [95, 96, 99], heavy rain [63, 65] with precip probability >= 70%, wind gusts >= 40mph, temp >= 105F. Returns `{ type, label, shouldReschedule }`.
    - `getTemperatureForToday(lat, lng)` — convenience function that fetches forecast and returns today's max temperature in Fahrenheit (for dosing modifier).

    Create `src/lib/chemistry/prediction.ts`:
    - `computeLinearTrend(values: number[]): TrendResult | null` — pure TypeScript OLS regression. Requires minimum 3 data points. Returns `{ slope, intercept, rSquared, projectedNext }`. Clamp input values to [0, 10000] before regression to avoid outlier sensitivity.
    - `TrendResult` interface exported.

    Create `src/lib/chemistry/__tests__/prediction.test.ts`:
    - Test computeLinearTrend with known linear data (slope=2, intercept=10), returns correct slope/intercept/R2.
    - Test returns null for fewer than 3 data points.
    - Test with constant values (slope=0, R2=1).
    - Test with noisy data (R2 < 1 but > 0).
    - Test outlier clamping.
  </action>
  <verify>Run `npx vitest run src/lib/chemistry/__tests__/prediction.test.ts` — all tests pass.</verify>
  <done>Open-Meteo client fetches and classifies weather; OLS regression computes correct trends with tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance dosing engine with weather and history modifiers</name>
  <files>src/lib/chemistry/dosing.ts</files>
  <action>
    Extend the existing `generateDosingRecommendations` function in `src/lib/chemistry/dosing.ts`:

    1. Add optional parameter `context?: { temperature_f?: number; historyReadings?: ChemistryReadings[]; poolId?: string }`.

    2. **Weather modifier**: When `temperature_f` is provided:
       - If temp > 90F: increase chlorine dose by 15% (heat accelerates chlorine burn-off).
       - If temp > 100F: increase chlorine dose by 25%.
       - If temp < 50F: decrease chlorine dose by 10% (cold slows burn-off).
       - Apply modifier only to chlorine-related products (sodium hypochlorite, cal-hypo, dichlor, trichlor).

    3. **History modifier**: When `historyReadings` is provided (array of past ChemistryReadings from most recent visits):
       - For each chemical parameter, extract the values array from history and call `computeLinearTrend()`.
       - If trend shows the parameter is consistently declining (slope < 0, R2 >= 0.4, projected next value < target.min * 0.9), increase the dose by 10% as a preemptive adjustment.
       - If trend shows parameter is consistently rising toward high (slope > 0, R2 >= 0.4, projected next > target.max * 1.1), reduce the dose by 10%.

    4. **Return enhanced result**: For each dosing recommendation, add an optional `modifiers?: { type: 'weather' | 'history'; label: string; adjustmentPct: number }[]` array. The UI will render these as badges per the user decision (AI as modifier badge on standard dose).

    5. Do NOT break the existing function signature — the `context` parameter is optional and all existing callers continue to work without it.
  </action>
  <verify>Run `npx vitest run src/lib/chemistry/__tests__/dosing.test.ts` — existing tests still pass. Manually verify that the new code compiles: `npx tsc --noEmit src/lib/chemistry/dosing.ts`.</verify>
  <done>Dosing engine accounts for weather temperature and per-pool history trends. Modifier badges are returned for each adjustment. Existing callers are unaffected.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/chemistry/` — all chemistry tests pass
- `npx tsc --noEmit` — no type errors across the project
- The Open-Meteo client can be tested with a simple `curl "https://api.open-meteo.com/v1/forecast?latitude=33.45&longitude=-112.07&daily=weather_code,temperature_2m_max&timezone=auto&forecast_days=1&temperature_unit=fahrenheit"` to confirm API response format
</verification>

<success_criteria>
- OLS regression correctly identifies trends in chemistry readings
- Dosing engine applies weather and history modifiers when context is provided
- All existing dosing tests still pass (backward compatible)
- Open-Meteo client fetches and classifies weather correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-01-SUMMARY.md`
</output>
