---
phase: 10-smart-features-ai
plan: 08
type: execute
wave: 3
depends_on: ["10-06"]
files_modified:
  - src/actions/weather.ts
  - src/lib/notifications/default-templates.ts
  - src/lib/db/schema/notification-templates.ts
autonomous: true
requirements:
  - SMART-07

must_haves:
  truths:
    - "When office approves a weather reschedule, affected customers automatically receive SMS and email about the delay"
    - "Notification includes the original date, new scheduled date, and weather reason"
    - "Office can uncheck individual customers before approving to exclude them from notification"
  artifacts:
    - path: "src/actions/weather.ts"
      provides: "Customer notification dispatch on proposal approval"
    - path: "src/lib/notifications/default-templates.ts"
      provides: "Default weather reschedule notification templates"
  key_links:
    - from: "src/actions/weather.ts"
      to: "src/actions/notification-templates.ts"
      via: "resolves weather_reschedule templates for customer notification"
      pattern: "getResolvedTemplate.*weather_reschedule"
---

<objective>
Weather delay customer notifications -- automatic SMS/email when stops are rescheduled due to weather.

Purpose: When office approves a weather reschedule proposal, affected customers should be automatically notified with the original date, new date, and reason. Per user decision: auto-notify with opt-out (office can uncheck individual customers before approving).

Output: Weather reschedule notification templates, notification dispatch on approval.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-smart-features-ai/10-06-SUMMARY.md
@src/actions/weather.ts
@src/actions/notification-templates.ts
@src/lib/notifications/default-templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weather reschedule notification templates and dispatch</name>
  <files>src/actions/weather.ts, src/lib/notifications/default-templates.ts</files>
  <action>
    **Add default templates** — Update `src/lib/notifications/default-templates.ts`:
    - Add `weather_reschedule_email` template:
      - Subject: "Schedule Update — {{company_name}}"
      - Body: "Hi {{customer_name}}, Due to {{weather_reason}} forecasted for {{original_date}}, we've rescheduled your pool service to {{new_date}}. We'll notify you when your tech is on the way. Thank you for your understanding. — {{company_name}}"
    - Add `weather_reschedule_sms` template:
      - SMS text: "{{company_name}}: Your {{original_date}} pool service has been rescheduled to {{new_date}} due to {{weather_reason}}. Reply STOP to unsubscribe."
    - Add both to `ALL_TEMPLATE_TYPES` array and `TEMPLATE_TYPE_META` with label/channel.

    **Wire notification dispatch into approveProposal** — Update `src/actions/weather.ts`:
    - After `approveProposal` executes the reschedules, dispatch notifications:
      1. Get the list of affected customers (from proposal.affected_stops), excluding any in `excluded_customer_ids`.
      2. For each customer with email or phone:
         - Resolve `weather_reschedule_email` and `weather_reschedule_sms` templates via `getResolvedTemplate()` with merge tags: `customer_name`, `company_name`, `weather_reason` (from proposal.weather_label), `original_date` (formatted), `new_date` (formatted).
         - If email template is enabled and customer has email, invoke the existing Edge Function pattern (similar to send-pre-arrival) for email delivery. Use Resend via the existing infrastructure.
         - If SMS template is enabled and customer has phone, invoke the existing Edge Function pattern for SMS delivery via Twilio.
      3. Fire-and-forget with `Promise.allSettled()` — notification failure must never roll back the reschedule.
      4. Log notification count in the proposal metadata for tracking.

    **Edge Function considerations**: If an existing Edge Function can handle generic email/SMS sends (check send-pre-arrival or send-invoice-sms patterns), reuse it with a new payload type. If not, create a minimal invocation that sends directly via Resend/Twilio from the server action (non-blocking).
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — compiles.</verify>
  <done>Approving a weather reschedule proposal triggers customer notifications via email and SMS with weather reason and new date. Templates are customizable through existing template system.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles successfully
- Default weather reschedule templates are added to the template system
- approveProposal dispatches notifications to non-excluded customers
- Notification failure does not block reschedule approval
</verification>

<success_criteria>
- Customers receive email + SMS when their stop is rescheduled due to weather
- Notifications include weather reason, original date, and new date
- Office can exclude individual customers from notification before approving
- Templates are customizable via existing notification settings
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-08-SUMMARY.md`
</output>
