---
phase: 10-smart-features-ai
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/service-visits.ts
  - src/actions/visits.ts
  - src/components/field/internal-notes.tsx
  - src/components/field/stop-card.tsx
  - src/app/(app)/customers/[id]/page.tsx
  - src/components/customers/service-history-timeline.tsx
autonomous: true
requirements:
  - NOTIF-05

must_haves:
  truths:
    - "Techs can leave internal service notes on stops that are visible only to office/owner, not the customer"
    - "Notes support flagging (needs follow-up, needs parts, safety concern)"
    - "Office sees internal notes on stop detail and customer timeline"
    - "Next tech on the route can see previous tech's notes"
  artifacts:
    - path: "src/components/field/internal-notes.tsx"
      provides: "Internal notes input with flag chips"
      exports: ["InternalNotes"]
    - path: "src/lib/db/schema/service-visits.ts"
      provides: "internal_notes and internal_flags columns on service_visits"
  key_links:
    - from: "src/components/field/stop-card.tsx"
      to: "src/components/field/internal-notes.tsx"
      via: "renders internal notes section in stop workflow"
      pattern: "InternalNotes"
    - from: "src/components/customers/service-history-timeline.tsx"
      to: "src/lib/db/schema/service-visits.ts"
      via: "displays internal notes for office/owner roles"
      pattern: "internal_notes"
---

<objective>
Internal service notes -- tech-to-office notes on stops, flagging system, next-tech handoff.

Purpose: Techs need to leave internal notes that are visible only to office/owner for flagging issues, requesting follow-up, or leaving messages for the next tech on the route. These are NOT customer-facing (per user requirement: "visible only to office/owner, NOT the customer").

Output: Internal notes columns, tech input UI, office-visible display on customer timeline.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db/schema/service-visits.ts
@src/components/field/stop-card.tsx
@src/components/field/completion-modal.tsx
@src/components/customers/service-history-timeline.tsx
@src/app/(app)/customers/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Internal notes schema and server action</name>
  <files>src/lib/db/schema/service-visits.ts, src/actions/visits.ts</files>
  <action>
    **Schema extension** — Add to service_visits table in `src/lib/db/schema/service-visits.ts`:
    - `internal_notes`: text, nullable. Free-text notes from tech to office.
    - `internal_flags`: jsonb, nullable. Array of flag strings, e.g., `["needs_follow_up", "needs_parts", "safety_concern", "handoff_note"]`.

    Run `drizzle-kit generate` to create the migration for the new columns.

    **Server action** — Update `src/actions/visits.ts` (or the stop completion action):
    - Extend `completeStop` to accept optional `internalNotes?: string` and `internalFlags?: string[]` parameters.
    - When present, save to the service_visit row alongside other completion data.
    - Also add `updateInternalNotes(visitId: string, notes: string, flags: string[])` for post-completion note editing (office can add notes after the fact too).
  </action>
  <verify>Run `npx drizzle-kit generate` — migration generated. Run `npx tsc --noEmit` — no type errors.</verify>
  <done>service_visits table has internal_notes and internal_flags columns. CompleteStop accepts and saves internal notes.</done>
</task>

<task type="auto">
  <name>Task 2: Internal notes UI for tech and office views</name>
  <files>src/components/field/internal-notes.tsx, src/components/field/stop-card.tsx, src/app/(app)/customers/[id]/page.tsx, src/components/customers/service-history-timeline.tsx</files>
  <action>
    **Internal notes component** — Create `src/components/field/internal-notes.tsx`:
    - A collapsible section titled "Internal Notes" with a small "Office only" badge.
    - Textarea for free-text notes.
    - Flag chips: "Needs Follow-up", "Needs Parts", "Safety Concern", "Handoff Note". Tappable toggle chips. Multiple flags can be selected.
    - If previous visit for this pool had internal notes, show a "Previous notes" collapsed section with the prior tech's notes and flags — helps with handoff.
    - Style: distinct from customer-facing sections. Use a subtle border or background tint to indicate "internal only."

    **Stop card integration** — Add InternalNotes to the stop workflow:
    - In the completion modal or the stop detail view (whichever is the final step before submission), add the InternalNotes section after checklist/chemistry.
    - Pass the data through to completeStop on submission.

    **Customer timeline (office view)** — Update `src/components/customers/service-history-timeline.tsx`:
    - For each visit that has internal_notes or internal_flags:
      - Show an "Internal Notes" section below the visit details.
      - Display the flags as colored badges: "Needs Follow-up" (amber), "Needs Parts" (blue), "Safety Concern" (red), "Handoff Note" (gray).
      - Display the notes text.
    - Only visible to owner and office roles — check user role before rendering this section.
    - Do NOT show internal notes on the customer portal or in any customer-facing view.

    **Customer page** — Update `src/app/(app)/customers/[id]/page.tsx`:
    - Ensure the service history query includes internal_notes and internal_flags in the fetched data.
    - Pass role to ServiceHistoryTimeline for conditional rendering.
  </action>
  <verify>Run `npm run build` — compiles. Visit a customer page in dev to confirm internal notes section renders for office role.</verify>
  <done>Techs can leave internal notes with flags during stop completion. Office sees notes on customer timeline. Notes are hidden from customers.</done>
</task>

</tasks>

<verification>
- `npm run build` compiles successfully
- Internal notes section appears in stop completion workflow
- Customer timeline shows internal notes for office/owner roles
- Notes are NOT visible in customer portal or customer-facing views
- Previous visit notes are shown for handoff context
</verification>

<success_criteria>
- Internal notes and flags are saved with service visits
- Techs can flag issues (needs follow-up, needs parts, safety concern)
- Office sees notes on customer timeline with flag badges
- Handoff notes from previous tech are visible to current tech
- Customer portal and customer-facing views never show internal notes
</success_criteria>

<output>
After completion, create `.planning/phases/10-smart-features-ai/10-15-SUMMARY.md`
</output>
