---
phase: 06-work-orders-quoting
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(app)/work-orders/page.tsx
  - src/app/(app)/work-orders/[id]/page.tsx
  - src/components/work-orders/wo-list.tsx
  - src/components/work-orders/wo-detail.tsx
  - src/components/work-orders/wo-create-dialog.tsx
  - src/components/shell/app-sidebar.tsx
  - src/app/(app)/layout.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02

must_haves:
  truths:
    - "Office staff can see a list of all work orders sorted by priority then date"
    - "Office staff can create a new work order from the WO list page"
    - "Office staff can view a WO detail page with timeline, line items, photos, and status controls"
    - "Work Orders appears in the sidebar for owner and office roles"
    - "WO detail page shows status transition buttons appropriate to current status"
  artifacts:
    - path: "src/app/(app)/work-orders/page.tsx"
      provides: "WO list page with filters"
      min_lines: 20
    - path: "src/app/(app)/work-orders/[id]/page.tsx"
      provides: "WO detail page"
      min_lines: 20
    - path: "src/components/work-orders/wo-list.tsx"
      provides: "Filterable WO list component"
      contains: "WoList"
    - path: "src/components/work-orders/wo-create-dialog.tsx"
      provides: "Create WO dialog with customer/pool selection"
      contains: "WoCreateDialog"
  key_links:
    - from: "src/app/(app)/work-orders/page.tsx"
      to: "src/actions/work-orders.ts"
      via: "server action import"
      pattern: "getWorkOrders"
    - from: "src/components/shell/app-sidebar.tsx"
      to: "/work-orders"
      via: "nav item href"
      pattern: "work-orders"
---

<objective>
Build the office-facing WO list page, WO detail page, WO create dialog, and add Work Orders to the sidebar navigation.

Purpose: Office staff need to view, create, and manage work orders. This is the primary WO management interface.
Output: /work-orders list page, /work-orders/[id] detail page, create dialog, sidebar nav item.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-work-orders-quoting/06-RESEARCH.md
@.planning/phases/06-work-orders-quoting/06-01-SUMMARY.md
@src/components/shell/app-sidebar.tsx
@src/app/(app)/layout.tsx
@src/app/(app)/customers/page.tsx
@src/app/(app)/alerts/page.tsx
@src/actions/work-orders.ts
@src/actions/customers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WO list page with filter chips, create dialog, and sidebar nav</name>
  <files>
    src/app/(app)/work-orders/page.tsx
    src/components/work-orders/wo-list.tsx
    src/components/work-orders/wo-create-dialog.tsx
    src/components/shell/app-sidebar.tsx
    src/app/(app)/layout.tsx
  </files>
  <action>
**Add sidebar nav item** in app-sidebar.tsx: Add a "Work Orders" entry with `WrenchIcon` from lucide-react, href `/work-orders`, roles `['owner', 'office']`. Place it after Dispatch in the nav order.

**Update layout.tsx** if needed: Ensure the work-orders route is included in any layout-level data fetching (e.g., if alert counts or WO counts are passed to sidebar).

**Create src/app/(app)/work-orders/page.tsx** — Server component page:
- Call `getWorkOrders()` to fetch initial WO list (no filters = all open WOs)
- Render page header: "Work Orders" title + "New Work Order" button (triggers WoCreateDialog)
- Render `<WoList workOrders={data} />`
- Export metadata: `{ title: 'Work Orders' }`

**Create src/components/work-orders/wo-list.tsx** — Client component:
- "use client" — needs interactive filter chips and status badge clicks
- Props: initial workOrders array
- Filter chips row: Status filter (Draft, Quoted, Approved, Scheduled, In Progress, Complete — toggleable), Priority filter (Emergency, High, Normal, Low), Tech filter (if techs exist). Use button-style chips with active/inactive states, matching the AlertFeed filter pattern.
- Each WO renders as a card/row with: priority badge (color-coded: emergency=red, high=amber, normal=blue, low=gray), title, customer name, category icon, status badge, assigned tech name (or "Unassigned"), target date, created date. Entire card is clickable → navigates to `/work-orders/{id}`.
- "Needs Attention" count shown above the list (draft WOs from tech flags + WOs with changes_requested quotes).
- Sort: emergency first, then high, then normal, then low, then by created_at desc (server already sorts this way).
- Use Next.js `useRouter` for client-side filter changes via `startTransition` + `router.refresh()` or maintain local filter state on the pre-fetched data.
- Dark-first design: dark cards, light text, colored priority/status badges.

**Create src/components/work-orders/wo-create-dialog.tsx** — Client component:
- Dialog (shadcn Dialog) with form fields:
  - Customer selector: dropdown or combobox searching customers by name. Fetch customers client-side or pass from server.
  - Pool selector: dropdown populated after customer selection (customer's pools).
  - Title: text input (required).
  - Description: textarea (optional).
  - Category: pill-button picker (pump, filter, heater, plumbing/leak, surface, electrical, other).
  - Priority: pill-button picker (low, normal, high, emergency).
  - Template selector (optional): dropdown of WO templates. When selected, pre-fills title, description, category, priority from template.
- Submit calls `createWorkOrder` server action. On success, toast + close dialog + router.refresh().
- Use plain React state + inline validation (no zod/hookform per project convention).
- Skip-quote shortcut: a checkbox "Skip quoting (small job)" that will be referenced later by the detail page.
  </action>
  <verify>
Navigate to `/work-orders` — page renders with empty state or existing WOs. "New Work Order" button opens dialog. Sidebar shows Work Orders for owner/office.
  </verify>
  <done>
WO list page renders with filter chips and priority-sorted WO cards. Create dialog allows customer/pool/title/category/priority selection. Sidebar nav includes Work Orders link for owner and office roles.
  </done>
</task>

<task type="auto">
  <name>Task 2: WO detail page with timeline, line items, status controls, and assignment</name>
  <files>
    src/app/(app)/work-orders/[id]/page.tsx
    src/components/work-orders/wo-detail.tsx
  </files>
  <action>
**Create src/app/(app)/work-orders/[id]/page.tsx** — Server component:
- Fetch WO via `getWorkOrder(id)` including customer, pool, line items, quotes, assigned tech, activity log.
- If not found, `notFound()`.
- Render `<WoDetail workOrder={data} />`.

**Create src/components/work-orders/wo-detail.tsx** — Client component with multiple sections:

**Header section:**
- Customer name (linked to /customers/[id]), pool name, category badge, priority badge (color-coded), current status badge.
- Edit button (opens inline edit for title, description, priority, category).

**Status action bar:**
- Contextual buttons based on current status:
  - Draft: "Create Quote" (navigates to quote builder — Plan 05), "Skip Quote → Approve" (confirmation dialog, calls updateWorkOrderStatus('approved')), "Cancel" (cancel dialog with reason field).
  - Quoted: "Cancel" (if quote pending).
  - Approved: "Schedule" (opens schedule dialog — tech picker + date picker, calls updateWorkOrderStatus('scheduled', { assignedTechId, targetDate })).
  - Scheduled: "Reassign" (tech picker), "Cancel".
  - In Progress: shows "In progress by [tech name]".
  - Complete: "Prepare Invoice" (navigates to invoice prep — Plan 07), "Create Follow-Up" (calls createFollowUpWorkOrder).
  - Invoiced: "View Invoice" link.
  - Cancelled: shows cancel reason + who cancelled.

**Assignment section:**
- Shows assigned tech name + target date (if scheduled).
- "Assign" or "Reassign" button: opens dialog with tech dropdown + date picker.
- Reassignment logs to activity_log.

**Line items section:**
- Table showing: description, type (part/labor), qty, unit, unit price, line total.
- "Add Line Item" button (opens inline form or dialog — Plan 04 builds the full editor; for now, show the list read-only with a placeholder "Add items" button).
- Shows subtotal, tax, discount, total calculations.
- Editable until status is 'invoiced'.

**Photos section:**
- Grid of photos (from completion_photo_paths or from tech flag photos).
- Each photo is clickable to view full-size.

**Activity timeline:**
- Chronological list of activity_log events.
- Each event shows: icon (based on type), description text, timestamp, actor name.
- Event types: created, status_changed, assigned, reassigned, note_added, quote_sent, quote_approved, quote_declined, changes_requested, completed, cancelled, follow_up_created, invoiced.
- Most recent event at top.

**Linked quotes section:**
- List of quotes associated with this WO (from getWorkOrder query).
- Each shows: quote number, version, status, sent date, amount.
- Clickable to view quote details (future plan wiring).

**Follow-up WOs section:**
- If parent_wo_id is set, show link to parent WO.
- If this WO has follow-ups (query by parent_wo_id), show links to child WOs.

**Styling:** Dark-first design with card sections. Priority-colored left border on header. Status badges use consistent color scheme: draft=gray, quoted=blue, approved=green, scheduled=purple, in_progress=amber, complete=emerald, invoiced=slate, cancelled=red.
  </action>
  <verify>
Navigate to `/work-orders/[id]` for an existing WO. Detail page shows all sections. Status buttons change based on WO state. Activity timeline renders events.
  </verify>
  <done>
WO detail page shows header with status/priority badges, status action bar with contextual buttons, assignment section, line items list, photos grid, activity timeline, linked quotes, and follow-up WO links. Status transitions work via button clicks.
  </done>
</task>

</tasks>

<verification>
- `/work-orders` page renders with filter chips and WO cards
- `/work-orders/[id]` detail page renders all sections
- Create WO dialog works end-to-end (select customer → fill form → submit → WO appears in list)
- Status transition buttons work (draft → approved via skip-quote, approved → scheduled with tech+date)
- Sidebar shows Work Orders for owner/office, hidden for tech
- Activity timeline shows creation and status change events
</verification>

<success_criteria>
Office staff can navigate to Work Orders, see a filtered/sorted list, create new WOs, view full WO details with timeline, and transition WO status through the workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/06-work-orders-quoting/06-02-SUMMARY.md`
</output>
