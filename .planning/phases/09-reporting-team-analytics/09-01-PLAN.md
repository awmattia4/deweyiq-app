---
phase: 09-reporting-team-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema/profiles.ts
  - src/lib/db/schema/chemical-products.ts
  - src/lib/db/schema/org-settings.ts
  - src/lib/db/schema/service-visits.ts
  - src/lib/db/schema/route-stops.ts
  - src/actions/visits.ts
  - src/components/reports/report-shared.tsx
  - src/app/(app)/reports/page.tsx
  - package.json
autonomous: true
requirements:
  - REPT-01
  - REPT-02
  - REPT-03
  - REPT-04
  - REPT-05
  - REPT-06

must_haves:
  truths:
    - "Owner navigating to /reports sees 7 tabs: AR Aging, Revenue (existing), P&L, Revenue Dashboard (new), Operations, Team, Profitability"
    - "Tech navigating to /reports sees a stripped-down 'My Performance' view instead of being redirected away"
    - "Recharts is installed and importable"
    - "profiles table has pay_type and pay_rate columns"
    - "chemical_products table has cost_per_unit column"
    - "org_settings table has chem_profit_margin_threshold_pct and wo_upsell_commission_pct columns"
    - "service_visits table has dosing_amounts JSONB column"
    - "route_stops table has started_at timestamp column"
    - "completeStop populates dosing_amounts on each visit"
    - "Shared chart color constants and time period selector component exist"
  artifacts:
    - path: "src/components/reports/report-shared.tsx"
      provides: "Shared chart colors, time period selector, CSV utility, KPI card component"
      exports: ["CHART_COLORS", "TimePeriodSelector", "downloadCsv", "KpiCard"]
    - path: "src/lib/db/schema/profiles.ts"
      provides: "pay_type and pay_rate columns on profiles"
      contains: "pay_type"
    - path: "src/lib/db/schema/chemical-products.ts"
      provides: "cost_per_unit column on chemical_products"
      contains: "cost_per_unit"
    - path: "src/lib/db/schema/org-settings.ts"
      provides: "chem_profit_margin_threshold_pct and wo_upsell_commission_pct"
      contains: "chem_profit_margin_threshold_pct"
    - path: "src/lib/db/schema/service-visits.ts"
      provides: "dosing_amounts JSONB column on service_visits"
      contains: "dosing_amounts"
    - path: "src/lib/db/schema/route-stops.ts"
      provides: "started_at timestamp on route_stops"
      contains: "started_at"
  key_links:
    - from: "src/actions/visits.ts"
      to: "src/lib/db/schema/service-visits.ts"
      via: "completeStop inserts dosing_amounts"
      pattern: "dosing_amounts"
    - from: "src/app/(app)/reports/page.tsx"
      to: "src/components/reports/report-shared.tsx"
      via: "imports shared components for new tabs"
      pattern: "report-shared"
---

<objective>
Install Recharts, add all Phase 9 schema columns, create shared reporting infrastructure, and extend the existing /reports page with the 4 new tab shells.

Purpose: All 4 subsequent plans depend on these schema fields, the charting library, and the shared UI components. This plan establishes the foundation so Plans 02-05 can execute sequentially without conflicts.
Output: Schema migration applied, Recharts installed, shared report components created, reports page restructured with 7 tabs (3 existing + 4 new).
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reporting-team-analytics/09-CONTEXT.md
@.planning/phases/09-reporting-team-analytics/09-RESEARCH.md
@src/lib/db/schema/profiles.ts
@src/lib/db/schema/chemical-products.ts
@src/lib/db/schema/org-settings.ts
@src/lib/db/schema/service-visits.ts
@src/lib/db/schema/route-stops.ts
@src/lib/db/schema/index.ts
@src/actions/visits.ts
@src/app/(app)/reports/page.tsx
@src/components/reports/revenue-report.tsx
@src/actions/reports.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts, add schema columns, update completeStop</name>
  <files>
    package.json
    src/lib/db/schema/profiles.ts
    src/lib/db/schema/chemical-products.ts
    src/lib/db/schema/org-settings.ts
    src/lib/db/schema/service-visits.ts
    src/lib/db/schema/route-stops.ts
    src/actions/visits.ts
  </files>
  <action>
    1. Install Recharts: `npm install recharts`

    2. Add to `src/lib/db/schema/profiles.ts` (after the `avatar_url` field):
       - `pay_type: text("pay_type").default("per_stop")` — 'per_stop' | 'hourly'
       - `pay_rate: numeric("pay_rate", { precision: 10, scale: 2 })` — dollar amount per stop or per hour
       Import `numeric` from `drizzle-orm/pg-core`.

    3. Add to `src/lib/db/schema/chemical-products.ts` (after the `is_active` field):
       - `cost_per_unit: numeric("cost_per_unit", { precision: 10, scale: 4 })` — cost per unit (floz or lbs)
       Import `numeric` from `drizzle-orm/pg-core`.

    4. Add to `src/lib/db/schema/org-settings.ts` (after the `portal_welcome_message` field, before `created_at`):
       - `chem_profit_margin_threshold_pct: numeric("chem_profit_margin_threshold_pct", { precision: 5, scale: 2 }).default("20")` — minimum % margin before flagging
       - `wo_upsell_commission_pct: numeric("wo_upsell_commission_pct", { precision: 5, scale: 2 }).default("0")` — commission % for tech-flagged WOs

    5. Add to `src/lib/db/schema/service-visits.ts` (after the `email_sent_at` field):
       - `dosing_amounts: jsonb("dosing_amounts")` — Array<{ chemical: string; productId: string; amount: number; unit: string }>
       This stores what the tech actually applied at each visit. Import already has `jsonb`.

    6. Add to `src/lib/db/schema/route-stops.ts` (after the `pre_arrival_sent_at` field, before `created_at`):
       - `started_at: timestamp("started_at", { withTimezone: true })` — set when tech marks stop in_progress; used for stop duration calculation
       Import `timestamp` is already imported.

    7. Run `npx drizzle-kit generate` to create the migration file, then `npx drizzle-kit push` to apply to the database.

    8. CRITICAL: After push, verify RLS policies were not corrupted. Run:
       ```sql
       SELECT tablename, policyname, qual, with_check FROM pg_catalog.pg_policies WHERE tablename IN ('profiles', 'chemical_products', 'org_settings', 'service_visits', 'route_stops');
       ```
       If any qual or with_check values are NULL, recreate from the schema file's pgPolicy definitions using ALTER POLICY.

    9. Update `completeStop` in `src/actions/visits.ts`:
       - In the insert values object (around line 594), add `dosing_amounts: input.dosingAmounts ?? null`
       - In the onConflictDoUpdate set (around line 615), add `dosing_amounts: input.dosingAmounts ?? null`
       - Add `dosingAmounts` to the `CompleteStopInput` interface: `dosingAmounts?: Array<{ chemical: string; productId: string; amount: number; unit: string }>`
       - This is an optional field — existing callers that don't pass it will write null, and the field workflow (stop-workflow.tsx) will be updated in a future plan to pass the calculated dosing amounts at completion time.

    10. Update the route stop status transition: In the action that marks a stop as "in_progress" (look in `src/actions/routes.ts` or similar), set `started_at: new Date()` when status changes to "in_progress". If no explicit in_progress transition exists, add started_at to the stop update in the field workflow's begin-stop action.
  </action>
  <verify>
    - `npm ls recharts` shows recharts installed
    - `npx drizzle-kit push` succeeds without errors
    - TypeScript compiles: `npx tsc --noEmit` (or at least the modified files have no type errors)
    - RLS policies verified via pg_catalog query — no NULL qual/with_check
  </verify>
  <done>All 6 schema columns exist in the database, Recharts is installed, completeStop writes dosing_amounts, and route_stops records started_at on in_progress transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared report infrastructure and extend reports page with new tabs</name>
  <files>
    src/components/reports/report-shared.tsx
    src/app/(app)/reports/page.tsx
  </files>
  <action>
    1. Create `src/components/reports/report-shared.tsx` — "use client" component file with shared reporting utilities:

       a. **CHART_COLORS** constant object (hex only, NO oklch — SVG cannot parse it):
          ```typescript
          export const CHART_COLORS = {
            primary: "#0ea5e9",   // sky-500
            secondary: "#22d3ee", // cyan-400
            tertiary: "#2dd4bf",  // teal-400
            warning: "#fcd34d",   // amber-300
            danger: "#f87171",    // red-400
            muted: "#475569",     // slate-600
            grid: "#1e293b",      // slate-800
            text: "#94a3b8",      // slate-400
            bg: "#0f172a",        // slate-900
          } as const
          ```

       b. **TimePeriodSelector** component — preset ranges + custom date range:
          - Presets: "This week", "This month", "Last month", "This quarter", "This year", "Last year", "Custom"
          - When "Custom" selected, show two date inputs (start, end)
          - Props: `{ startDate: string; endDate: string; onChange: (start: string, end: string) => void }`
          - Use shadcn Select for the preset dropdown, regular date inputs for custom range
          - Use `toLocalDateString` from `@/lib/date-utils` for all date calculations (NEVER use toISOString)
          - Calculate preset ranges using plain Date math (startOfWeek = Sunday, startOfMonth = 1st, etc.)

       c. **KpiCard** component — reusable metric card:
          - Props: `{ title: string; value: string; subtitle?: string; trend?: { value: number; label: string }; icon?: React.ReactNode }`
          - Shows title, large value, optional subtitle, optional trend arrow (green ArrowUpRight for positive, red ArrowDownRight for negative)
          - Use Card component from shadcn, consistent with app's dark-first design
          - Trend indicator: `text-emerald-400` for positive, `text-red-400` for negative

       d. **downloadCsv** utility function:
          ```typescript
          export function downloadCsv(csv: string, filename: string) {
            const blob = new Blob([csv], { type: "text/csv;charset=utf-8;" })
            const url = URL.createObjectURL(blob)
            const link = document.createElement("a")
            link.href = url
            link.download = filename
            link.click()
            URL.revokeObjectURL(url)
          }
          ```

       e. **formatCurrency** utility: `(amount: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(amount)`

       f. **formatPercent** utility: `(value: number, decimals = 1) => value.toFixed(decimals) + "%"`

    2. Update `src/app/(app)/reports/page.tsx`:

       a. Change the tech redirect logic:
          - REMOVE: `if (user.role === "tech") redirect("/routes")`
          - ADD: If user is tech, render a stripped-down view that only shows the "My Scorecard" component (to be built in Plan 04). For now, render a placeholder div: `<div className="text-muted-foreground text-sm italic">Your scorecard will appear here.</div>` with the page title "My Performance".

       b. Restructure the TabsList from 3 columns to a scrollable horizontal layout (7 tabs won't fit in a grid on mobile):
          - Change from `grid grid-cols-3` to `flex w-full overflow-x-auto` on the TabsList
          - Keep existing 3 tabs: "AR Aging", "Revenue", "P&L"
          - Add 4 new tabs per user's locked decision: "Revenue" (revenue dashboard — NOTE: this is a DIFFERENT tab from the existing "Revenue" tab; label this new one "Revenue Dashboard" or use a distinct value like "revenue-dashboard" to differentiate from the existing "Revenue" tab), "Operations", "Team", "Profitability"
          - IMPORTANT: The user's locked decision specifies tab names as "Revenue, Operations, Team, Profitability" — do NOT use "Dashboard" as a tab name. If the existing "Revenue" tab creates a naming conflict, use "Revenue Dashboard" for the new Phase 9 tab to distinguish it while honoring the locked decision.
          - Existing tabs keep their current components
          - New tabs get placeholder TabsContent with the text: "Coming soon — Phase 9 Plan XX" (these will be filled by Plans 02-05)

       c. The existing data fetches (getArAging, getRevenueByCustomer, getPnlReport, getExpenses) remain unchanged. Do NOT add Phase 9 data fetches yet — each subsequent plan adds its own.

       d. Keep the `isOwner` variable and pass it to new tab components. The 4 new tabs are owner-only (team tab also visible to office).
  </action>
  <verify>
    - `npm run build` succeeds (or dev server starts without errors)
    - Navigate to /reports as owner — see 7 tabs, first 3 work as before, last 4 show placeholder text
    - The new revenue tab is labeled "Revenue Dashboard" (NOT "Dashboard") per user's locked decision
    - Navigate to /reports as tech — see stripped-down "My Performance" view (placeholder for now)
    - Import `{ CHART_COLORS, TimePeriodSelector, KpiCard, downloadCsv }` from report-shared resolves without errors
  </verify>
  <done>Reports page shows 7 tabs (3 existing + 4 new placeholders) with tab names matching the user's locked decision (Revenue Dashboard, Operations, Team, Profitability). Shared infrastructure (CHART_COLORS, TimePeriodSelector, KpiCard, downloadCsv, formatCurrency, formatPercent) is importable from report-shared.tsx. Tech users see a stripped-down performance view instead of being redirected.</done>
</task>

</tasks>

<verification>
- All 6 new schema columns exist in the database
- Recharts installed and importable
- Reports page renders with 7 tabs, new tabs labeled per locked decision (Revenue Dashboard, Operations, Team, Profitability)
- Tech users are not redirected away from /reports
- completeStop writes dosing_amounts field
- route_stops records started_at on in_progress
- Shared chart infrastructure components exist and are importable
- No TypeScript errors, no build failures
</verification>

<success_criteria>
Schema foundation is in place for all Phase 9 reporting queries. Recharts is ready for chart components. The reports page is restructured to accommodate 4 new tabs with names matching the user's locked decision. Shared components (chart colors, time selector, KPI cards, CSV download) are ready for Plans 02-05 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/09-reporting-team-analytics/09-01-SUMMARY.md`
</output>
