---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dashboard/loading.tsx
  - src/app/(app)/routes/page.tsx
  - src/app/(app)/routes/loading.tsx
  - src/app/(app)/team/page.tsx
  - src/app/(app)/team/loading.tsx
  - src/app/(app)/settings/page.tsx
  - src/app/portal/layout.tsx (update)
  - src/app/portal/page.tsx
  - src/components/shell/app-shell.tsx
  - src/components/shell/app-sidebar.tsx
  - src/components/shell/app-header.tsx
  - src/components/shell/portal-shell.tsx
  - proxy.ts (update)
autonomous: false
requirements:
  - AUTH-05

must_haves:
  truths:
    - "A tech user lands directly on /routes (today's route list) — no dashboard in between"
    - "An owner/office user lands on /dashboard with key metrics and quick actions"
    - "Owner sees additional tabs (billing settings, team management, reports) that office does not see"
    - "A tech cannot navigate to /dashboard — they are redirected to /routes"
    - "A customer cannot see staff routes or the dashboard — they only see /portal"
    - "Cross-org resource access returns 403 (enforced by RLS at DB level)"
    - "The app shell includes the offline banner and sync status icon"
    - "Every page shows skeleton screens during loading (not blank white or spinner)"
    - "The app is installable as a PWA on iPhone and Android"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "Protected staff layout with auth guard and role check"
      contains: "getClaims"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Owner/office dashboard landing"
      contains: "dashboard"
    - path: "src/app/(app)/routes/page.tsx"
      provides: "Tech route list landing"
      contains: "routes"
    - path: "src/components/shell/app-shell.tsx"
      provides: "App shell with sidebar, header, offline banner, sync icon"
      contains: "OfflineBanner"
    - path: "src/components/shell/app-sidebar.tsx"
      provides: "Role-aware sidebar navigation"
      contains: "role"
    - path: "proxy.ts"
      provides: "Role-based route protection"
      contains: "user_role"
  key_links:
    - from: "proxy.ts"
      to: "src/app/(app)/layout.tsx"
      via: "Proxy validates JWT + role before layout renders"
      pattern: "getClaims.*user_role"
    - from: "src/components/shell/app-shell.tsx"
      to: "src/components/shell/offline-banner.tsx"
      via: "Shell includes OfflineBanner component"
      pattern: "OfflineBanner"
    - from: "src/components/shell/app-shell.tsx"
      to: "src/components/shell/sync-status-icon.tsx"
      via: "Header includes SyncStatusIcon"
      pattern: "SyncStatusIcon"
    - from: "src/components/shell/app-sidebar.tsx"
      to: "src/actions/auth.ts"
      via: "Reads user role to show/hide nav items"
      pattern: "getCurrentUser.*role"
---

<objective>
Build the role-based app shell with sidebar navigation, role-aware routing, protected layouts, skeleton loading screens, and the minimal landing pages for each role — integrating the offline banner and sync icon from Plan 04.

Purpose: AUTH-05 (role-based permissions) is enforced through the combination of proxy.ts route guards, layout-level auth checks, and role-aware UI. Each role sees only what they are permitted to see. This plan wires together everything from Plans 01-04 into a usable application shell.

Output: A fully protected, role-aware app with: dashboard (owner/office), routes page (tech), portal shell (customer), sidebar navigation that adapts per role, skeleton loading screens on every page, and offline/sync indicators integrated into the shell.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build app shell, sidebar, header, and role-based proxy routing</name>
  <files>
    src/app/(app)/layout.tsx
    src/components/shell/app-shell.tsx
    src/components/shell/app-sidebar.tsx
    src/components/shell/app-header.tsx
    src/components/shell/portal-shell.tsx
    src/app/portal/layout.tsx
    proxy.ts
  </files>
  <action>
    1. Update `proxy.ts` to implement full role-based routing:
       - After `getClaims()`, read `user_role` from the JWT claims (set by Custom Access Token Hook).
       - Route protection rules:
         a. Unauthenticated + hitting `/(app)/*` -> redirect to `/login`
         b. Unauthenticated + hitting `/portal/*` (except `/portal/login`) -> redirect to `/portal/login`
         c. Authenticated tech + hitting `/dashboard` or `/team` or `/settings` -> redirect to `/routes`
         d. Authenticated customer + hitting `/(app)/*` -> redirect to `/portal`
         e. Authenticated owner/office + hitting `/login` or `/signup` -> redirect to `/dashboard`
         f. Authenticated tech + hitting `/login` -> redirect to `/routes`
         g. Authenticated customer + hitting `/login` -> redirect to `/portal`
       - Allow all authenticated staff roles (owner, office, tech) to access `/(app)/*` routes (tech restrictions handled at layout/page level for finer control).
       - Actually, simpler approach: proxy handles auth check and role-landing redirect only. Page-level protection in layout.tsx for role restrictions.

    2. Create `src/app/(app)/layout.tsx` — protected staff layout:
       - Server component.
       - Calls `getCurrentUser()` from auth actions.
       - If not authenticated, redirect to /login (defense-in-depth, proxy should catch this first).
       - If role is 'customer', redirect to /portal.
       - Pass user data to the shell as props.
       - Renders `<AppShell user={user}>` wrapping `{children}`.
       - Initialize sync listener on mount (via a client component wrapper that calls `initSyncListener()` from Plan 04).

    3. Create `src/components/shell/app-shell.tsx` (client component wrapper with server-passed data):
       - Layout structure:
         - Sidebar on left (collapsible on mobile)
         - Header at top (with sync icon, user avatar, sign-out)
         - OfflineBanner at very top (fixed, above everything)
         - Main content area
       - Uses shadcn sidebar component pattern: `npx shadcn@latest add sidebar sheet avatar dropdown-menu`.
       - Responsive: sidebar collapses to hamburger menu on mobile.
       - Per user decision: "bold & modern — deep blue or teal, Linear/Vercel aesthetic, sharp and minimal."

    4. Create `src/components/shell/app-sidebar.tsx`:
       - Navigation items vary by role:
         - **Tech**: Routes (today's stops), Profile — that's it, minimal.
         - **Office**: Dashboard, Customers (Phase 2), Routes, Schedule (Phase 4), Team, Settings.
         - **Owner**: Everything office sees PLUS: Billing (Phase 7), Reports (Phase 9), Team Management.
       - Items for future phases show as disabled/coming-soon or are hidden entirely (Claude's discretion — suggest hiding to keep it clean, with a note in the summary about which items to enable per phase).
       - Per user decision: "Owner and office share the same view; owner gets additional tabs (billing settings, team management, reports)."
       - Active state indicator on current route.
       - User avatar and name at bottom of sidebar.
       - Sign out button.

    5. Create `src/components/shell/app-header.tsx`:
       - Breadcrumb or page title area.
       - Right side: SyncStatusIcon (from Plan 04), user avatar with dropdown (profile, sign out).
       - Mobile: hamburger menu trigger for sidebar.

    6. Update `src/app/portal/layout.tsx`:
       - Wrap portal pages with `<PortalShell>`.
       - Auth check: if not authenticated, redirect to /portal/login.
       - If role is not 'customer', redirect to /dashboard (staff shouldn't be in portal).

    7. Create `src/components/shell/portal-shell.tsx`:
       - Simpler than staff shell — no sidebar, just header + content.
       - Header: company logo placeholder (per user decision: "customer portal shows the pool company's own logo and colors"), navigation links (Service History, Invoices, Messages — all stubbed for Phase 8).
       - Per user decision: "Branding model: product brand for staff experience; customer portal shows the pool company's own logo and colors."
       - Phase 1: use placeholder branding. The portal shell structure is ready, company-specific branding loads from org settings in future phases.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `npm run build` succeeds.
    proxy.ts correctly routes by role (review the redirect logic).
    AppShell includes OfflineBanner and SyncStatusIcon.
    Sidebar shows different nav items based on role.
    Portal layout redirects non-customers away.
  </verify>
  <done>
    Role-based app shell is complete. proxy.ts enforces route access by role. Staff see the sidebar + header shell with role-appropriate navigation. Customers see a separate portal shell. Offline banner and sync icon are integrated into the staff shell. All route protection layers work (proxy -> layout -> page).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build role landing pages with real data and skeleton loading screens</name>
  <files>
    src/app/(app)/dashboard/page.tsx
    src/app/(app)/dashboard/loading.tsx
    src/app/(app)/routes/page.tsx
    src/app/(app)/routes/loading.tsx
    src/app/(app)/team/page.tsx
    src/app/(app)/team/loading.tsx
    src/app/(app)/settings/page.tsx
    src/app/portal/page.tsx
  </files>
  <action>
    1. Create `src/app/(app)/dashboard/page.tsx` — owner/office landing:
       - Server component. Fetches real data where available:
         a. User's profile (name, role) — from profiles table via Drizzle with RLS.
         b. Team member count for the org — from profiles table.
         c. Org name — from orgs table.
       - Layout per user decision: "Dashboard should feel like a command center, not a report — key metrics and quick actions, not charts."
       - Cards with: Today's stops count (placeholder "0 — set up routes in Phase 4"), team member count (real), recent activity (empty state with message).
       - Quick action buttons: "Invite Team Member" (links to team page), "View Routes" (placeholder).
       - Per user decision: "Phase 1 pages: minimal real content — basic dashboard with real data where available (user profile, team list), not just placeholder pages."
       - Role guard: if tech, redirect to /routes. If customer, redirect to /portal.
       - Use shadcn Card, Badge components.

    2. Create `src/app/(app)/dashboard/loading.tsx` — skeleton screen:
       - Per user decision: "Loading experience: skeleton screens (show layout structure with gray placeholders immediately), not splash screen."
       - Grid of skeleton cards matching the dashboard layout.
       - Quick action row with skeleton buttons.
       - Uses shadcn Skeleton component.

    3. Create `src/app/(app)/routes/page.tsx` — tech landing:
       - Server component. This is the tech's primary page.
       - Per user decision: "Tech: lands directly on today's route list — no dashboard in between."
       - Per idea: "Tech landing should feel instant — open app, see your route with zero navigation required."
       - Phase 1 content: empty state with clear message — "No routes scheduled yet. Routes will appear here once scheduling is set up in a future update."
       - Show the tech's profile info (name, role).
       - Date header showing today's date.
       - Layout ready for the stop list cards (Phase 3).
       - Role guard: allow tech, owner, office. Redirect customer to /portal.

    4. Create `src/app/(app)/routes/loading.tsx` — skeleton for route list:
       - Date header skeleton.
       - List of 5-6 skeleton cards (representing stop cards).
       - Each card skeleton shows: address line, customer name line, time slot.

    5. Create `src/app/(app)/team/page.tsx`:
       - Server component. Real data: lists all profiles in the org from the profiles table (via Drizzle with RLS).
       - Shows: name, email, role badge, joined date.
       - "Invite Member" button that opens a form/dialog calling the invite action from Plan 03.
       - Per user decision: real data where available — the team list IS real data.
       - Only accessible to owner and office roles.

    6. Create `src/app/(app)/team/loading.tsx`:
       - Table/list skeleton with rows.

    7. Create `src/app/(app)/settings/page.tsx`:
       - Minimal settings page. Shows: org name, user's profile info (editable), sign out button.
       - Only accessible to owner and office.
       - Phase 1: basic profile editing only. Org settings expand in later phases.

    8. Create `src/app/portal/page.tsx`:
       - Customer portal landing. Phase 1: welcome message with customer's name.
       - Empty states for: Service History, Invoices, Messages — all with "Coming soon" messaging.
       - Per user decision: customer portal has company branding (placeholder in Phase 1).

    All pages must:
    - Use the Tailwind v4 design system (dark theme, deep blue/teal)
    - Use shadcn components consistently
    - Have corresponding loading.tsx with skeleton screens
    - Fetch data via server components using Drizzle with RLS wrapper (createRlsClient or withRls from Plan 02)
    - Handle empty states gracefully with helpful messages
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    `npm run build` succeeds.
    Dashboard page renders with real team count and org name.
    Routes page renders with empty state for tech role.
    Team page lists org members from database.
    Every page has a corresponding loading.tsx with skeleton screens.
    Portal page renders for customer role.
    Role guards redirect unauthorized roles.
  </verify>
  <done>
    All role landing pages are built with real data where available: dashboard shows team count and org name, tech routes page shows today's date with empty-state, team page lists real org members with invite button, settings shows profile, and portal shows customer welcome. Every page has skeleton loading screens. Role guards enforce AUTH-05 at the page level.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 Foundation</name>
  <files>N/A — verification only, no files modified</files>
  <action>
    CHECKPOINT: Human verification of the complete Phase 1 foundation.

    What was built: Complete Phase 1 foundation — Next.js 16 app with auth (signup, login, password reset, invite, Google OAuth), multi-tenant Postgres schema with RLS, PWA installable shell with offline indicators, and role-based routing with protected pages.

    Verification steps for the user:

    1. Sign up: Visit /signup. Create an account with your name, email, password, and a company name. Confirm you receive a verification email.

    2. Log in: Visit /login. Sign in with your credentials. Confirm you land on the dashboard (as owner).

    3. Session persistence: Refresh the page. Confirm you remain logged in.

    4. Dashboard: Confirm the dashboard shows your org name, team member count (1 — you), and quick actions. Confirm it feels like a "command center" — cards, not charts.

    5. Team page: Navigate to /team. Confirm you see yourself listed. Click "Invite Member", enter a test email with the "tech" role. Confirm the invite email is sent.

    6. Invite flow: Open the invite email (in a different browser or incognito). Click the link. Confirm you can set a password and are redirected to /routes (tech landing).

    7. Tech role restrictions: As the tech user, try navigating to /dashboard. Confirm you are redirected back to /routes. Confirm the sidebar shows only Routes and Profile.

    8. Password reset: Sign out. Visit /reset-password. Enter your email. Confirm you receive a reset email. Click the link and set a new password. Log in with the new password.

    9. Offline banner: In Chrome DevTools, go to Network tab and set to "Offline". Confirm a thin colored banner appears. Set back to "Online" — confirm the banner disappears.

    10. Sync icon: Confirm the sync status icon is visible in the header. It should show "All synced" (green/muted state).

    11. PWA install: On a mobile device (or Chrome desktop), confirm the app offers to install / can be added to home screen. Confirm the manifest shows the correct app name and theme color.

    12. Skeleton screens: Navigate between pages and confirm skeleton loading screens appear briefly before content loads.

    13. Portal login: Visit /portal/login. Confirm it has distinct styling from the staff login. (Customer accounts tested in Phase 8.)

    14. Visual design: Confirm the overall look matches the direction: deep blue/teal, bold and modern, Linear/Vercel aesthetic, sharp and minimal.

    Resume signal: Type "approved" or describe any issues to fix.
  </action>
  <verify>User confirms all 14 verification steps pass, or provides specific issues to address.</verify>
  <done>Phase 1 Foundation is verified by the user. All auth flows, role-based routing, offline indicators, PWA installation, and visual design meet the requirements and locked decisions.</done>
</task>

</tasks>

<verification>
- All 5 success criteria from ROADMAP Phase 1 are met:
  1. User can sign up, log in, remain logged in across browser refreshes and device restarts
  2. User can reset forgotten password via email link
  3. Owner can invite team member with role; invitee receives email and activates
  4. Each role sees only permitted content; cross-org access returns 403 (RLS)
  5. App installable as PWA, loads shell offline, queues writes for sync on reconnect
- `npm run build` succeeds with no errors
- `npx tsc --noEmit` passes with no type errors
- All AUTH-01 through AUTH-06 requirements are satisfied
</verification>

<success_criteria>
Phase 1 Foundation is complete. The platform has: working authentication with all flows (signup, login, OAuth, reset, invite), multi-tenant isolation at the database level via RLS, an installable PWA with offline-first architecture, and role-based routing that enforces permissions. Every subsequent phase can build on this infrastructure without retrofitting any of these foundational concerns.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
