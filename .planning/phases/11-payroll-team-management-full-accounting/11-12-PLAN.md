---
phase: 11-payroll-team-management-full-accounting
plan: 12
type: execute
wave: 4
depends_on: ["11-06", "11-07", "11-10", "11-11"]
files_modified:
  - src/actions/accounting.ts
  - src/actions/vendor-bills.ts
  - src/components/accounting/sales-tax-manager.tsx
  - src/components/accounting/period-close.tsx
  - src/components/accounting/audit-trail.tsx
  - src/components/accounting/ap-workflow.tsx
  - src/lib/accounting/journal.ts
autonomous: true
requirements:
  - ACCT-10
  - ACCT-13
  - ACCT-14
must_haves:
  truths:
    - "Sales tax tracking per jurisdiction with configurable rates"
    - "Owner can close accounting periods to prevent backdated changes"
    - "All journal entries are immutable — corrections via reversal only"
    - "Audit trail shows who created/modified financial records and when"
    - "Closed periods cannot have new journal entries posted to them"
    - "Owner can enter vendor bills with due dates and amounts"
    - "Owner can schedule payments for vendor bills"
    - "AP aging report shows overdue vendor bills by time bucket"
  artifacts:
    - path: "src/components/accounting/sales-tax-manager.tsx"
      provides: "Sales tax rate configuration and reporting"
      contains: "SalesTaxManager"
    - path: "src/components/accounting/period-close.tsx"
      provides: "Period close workflow UI"
      contains: "PeriodClose"
    - path: "src/components/accounting/audit-trail.tsx"
      provides: "Financial audit trail viewer"
      contains: "AuditTrail"
    - path: "src/actions/vendor-bills.ts"
      provides: "Vendor bill CRUD, payment scheduling, AP aging"
      exports: ["createVendorBill", "getVendorBills", "schedulePayment", "getApAging"]
    - path: "src/components/accounting/ap-workflow.tsx"
      provides: "Accounts payable management UI"
      contains: "ApWorkflow"
  key_links:
    - from: "src/actions/accounting.ts"
      to: "src/lib/db/schema/accounting.ts"
      via: "accounting_periods table for period close"
      pattern: "accountingPeriods"
    - from: "src/components/accounting/period-close.tsx"
      to: "src/actions/accounting.ts"
      via: "closePeriod, getPeriods actions"
      pattern: "closePeriod|getAccountingPeriods"
    - from: "src/actions/vendor-bills.ts"
      to: "src/lib/accounting/journal.ts"
      via: "Auto-generate journal entry on vendor bill creation"
      pattern: "createJournalEntry"
    - from: "src/components/accounting/ap-workflow.tsx"
      to: "src/actions/vendor-bills.ts"
      via: "Vendor bill CRUD and AP aging data"
      pattern: "createVendorBill|getApAging"
---

<objective>
Add sales tax management, period close, audit trail, and accounts payable workflow to the accounting system.

Purpose: Pool companies need to track sales tax by jurisdiction, close periods for clean financial reporting, maintain an immutable audit trail for CPA review, and manage vendor bills with payment scheduling and AP aging reports.
Output: Sales tax configuration, period close workflow, audit trail viewer, vendor bill entry, payment scheduling, AP aging report.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-payroll-team-management-full-accounting/11-06-SUMMARY.md
@.planning/phases/11-payroll-team-management-full-accounting/11-07-SUMMARY.md
@.planning/phases/11-payroll-team-management-full-accounting/11-10-SUMMARY.md
@.planning/phases/11-payroll-team-management-full-accounting/11-11-SUMMARY.md
@src/actions/accounting.ts
@src/lib/db/schema/accounting.ts
@src/lib/db/schema/team-management.ts
@src/lib/accounting/journal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sales tax, period close, audit trail, and vendor bill actions</name>
  <files>
    src/actions/accounting.ts
    src/actions/vendor-bills.ts
    src/lib/accounting/journal.ts
  </files>
  <action>
    Extend `src/actions/accounting.ts` with:

    **Sales Tax:**
    1. `getSalesTaxRates()` — Owner only. Returns configured tax rates. Use the existing `org_settings.default_tax_rate` as the base. For multi-jurisdiction: store additional rates in a JSONB column on org_settings (e.g., `sales_tax_rates: [{ jurisdiction: "County A", rate: 0.07 }, ...]`). If no custom rates, use the single default rate.
    2. `updateSalesTaxRates(rates: { jurisdiction: string; rate: number }[])` — Owner only. Updates the tax rate configuration.
    3. `getSalesTaxReport(startDate: string, endDate: string)` — Owner only. Summarizes sales tax collected (from invoice tax amounts via journal_entry_lines on account 2100 - Sales Tax Payable) by period. Shows: total tax collected, total tax owed, net (collected - remitted).

    **Period Close:**
    4. `getAccountingPeriods()` — Owner only. Returns all accounting_periods, ordered by period_start DESC. Shows status (open/closed), date range, closed_at, closed_by name.
    5. `createAccountingPeriod(periodStart: string, periodEnd: string)` — Owner only. Creates a new period. Validates no overlap with existing periods.
    6. `closePeriod(periodId: string)` — Owner only. Sets status='closed', closed_at=now(), closed_by=current user. After closing, the `createJournalEntry` function (from Plan 06) must check if the entry_date falls within a closed period and reject if so.
    7. `reopenPeriod(periodId: string)` — Owner only. Sets status='open', clears closed_at/closed_by. Safety net for corrections.

    **Audit Trail:**
    8. `getAuditTrail(filters: { entityType?, entityId?, userId?, startDate?, endDate? })` — Owner only. Returns a chronological log of financial actions. Since journal entries are immutable (created, never updated), the audit trail IS the journal_entries table itself — sorted by created_at, showing: date, description, source_type, source_id, created_by name, amount, is_reversed. Reversals link to the original entry.
    9. For non-journal events (period close, tax rate changes, account creation), add a lightweight `accounting_events` logging mechanism — either a dedicated table or entries in the existing alerts table with a specific type. Store: event_type, description, user_id, timestamp, metadata JSONB.

    Update the existing `createJournalEntry` in `src/lib/accounting/journal.ts` to check for closed periods:
    - Before inserting, query accounting_periods for the entry_date. If a closed period contains the entry_date, throw an error: "Cannot post to closed period {periodStart} - {periodEnd}".

    **New file: `src/actions/vendor-bills.ts`** — Accounts Payable workflow (ACCT-10):

    The `vendors` table already exists (Plan 01, in team-management.ts). This file adds vendor bill management on top of it.

    1. `createVendorBill(input: { vendorId: string, billNumber?: string, billDate: string, dueDate: string, description: string, amount: string, categoryAccountId?: string })` — Owner only. Creates an expense record linked to a vendor with source_type='vendor_bill', status='unpaid', and the given due_date. Auto-generates journal entry: Dr Expense account (based on category or default 5000), Cr Accounts Payable (2000). Returns the created bill.

    2. `getVendorBills(filters?: { vendorId?: string, status?: 'unpaid'|'scheduled'|'paid'|'overdue', startDate?: string, endDate?: string })` — Owner/office only. Returns vendor bills with vendor name, amount, due date, status, days until/past due. Bills past due_date with status='unpaid' are flagged as 'overdue' at query time.

    3. `updateVendorBill(billId: string, updates: { dueDate?, description?, amount?, categoryAccountId? })` — Owner only. Updates unpaid bill details. If amount changes, reverses old journal entry and creates new one.

    4. `schedulePayment(billId: string, scheduledDate: string)` — Owner only. Sets bill status to 'scheduled' and records the scheduled payment date. Does NOT create a journal entry yet — that happens when the payment is actually recorded.

    5. `recordBillPayment(billId: string, paymentMethod: 'check'|'bank_transfer'|'cash', referenceNumber?: string)` — Owner only. Marks bill as 'paid'. Creates journal entry: Dr Accounts Payable (2000), Cr Checking/Bank (1000). Updates the expense record with payment details.

    6. `getApAging()` — Owner only. Returns AP aging buckets: current (not yet due), 1-30 days overdue, 31-60, 61-90, 90+ days overdue. Each bucket includes vendor name, bill number, amount, due date. Also returns totals per bucket and grand total. This is the dedicated AP aging report for ACCT-10.

    7. `getApSummary()` — Owner only. Quick summary: total AP outstanding, total overdue, bills due this week, bills due this month. Used by the AP workflow dashboard card.
  </action>
  <verify>
    1. `npm run build` passes
    2. Period close prevents new journal entries in that period
    3. Sales tax report aggregates correctly from journal entries
    4. Audit trail returns chronological financial event log
    5. Vendor bill creation generates AP journal entry (Dr Expense, Cr AP)
    6. AP aging correctly buckets overdue bills
    7. Bill payment creates reversal AP entry (Dr AP, Cr Bank)
  </verify>
  <done>
    Sales tax rates configurable per jurisdiction. Period close prevents backdated changes. Audit trail shows complete financial event history via immutable journal entries. Reopening periods available as safety net. Vendor bills can be entered with due dates, payments can be scheduled and recorded, and AP aging report shows overdue amounts by time bucket.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build sales tax, period close, and audit trail UI</name>
  <files>
    src/components/accounting/sales-tax-manager.tsx
    src/components/accounting/period-close.tsx
    src/components/accounting/audit-trail.tsx
  </files>
  <action>
    **New file: `src/components/accounting/sales-tax-manager.tsx`** — Client component:
    - Tax rates table: jurisdiction name, rate %, actions (edit/delete).
    - "Add Tax Rate" form: jurisdiction name, rate input (use controlled decimal input per MEMORY.md).
    - Tax summary section: this quarter's tax collected, tax owed, net.
    - Wire into Accounting page as a sub-section or accessible from the Chart of Accounts tab.

    **New file: `src/components/accounting/period-close.tsx`** — Client component:
    - Periods list: each period shows start-end dates, status badge (open=green, closed=amber).
    - "Close Period" button: confirmation dialog ("Closing this period will prevent posting journal entries dated within {start} - {end}. Continue?"). Calls closePeriod.
    - "Create Period" form: start date, end date pickers. Validates no overlap.
    - "Reopen" option on closed periods (with warning).
    - Accountant mode only (check org_settings.accountant_mode_enabled).
    - Wire into Accounting page as a sub-tab under the accountant mode section.

    **New file: `src/components/accounting/audit-trail.tsx`** — Client component:
    - Chronological list of financial events (journal entries + accounting events).
    - Columns: Date, Action, Description, User, Amount, Source.
    - Filter by: date range, event type, user.
    - Reversed entries shown with strikethrough and "Reversed" badge.
    - Reversal entries show "Reversal of: {original description}" with link.
    - Accountant mode only.
    - Wire into Accounting page.

    All three components are integrated into the Accounting page tabs (within accountant mode section or as always-visible depending on the component — SalesTaxManager is visible in simplified mode too, PeriodClose and AuditTrail are accountant-mode only).
  </action>
  <verify>
    1. `npm run build` passes
    2. Sales tax manager renders with rate editing
    3. Period close workflow completes with confirmation
    4. Audit trail shows chronological financial events
  </verify>
  <done>
    Sales tax management with per-jurisdiction rates and quarterly summary. Period close workflow with confirmation and reopen safety net. Audit trail showing complete financial history. All integrated into the accounting page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build accounts payable workflow UI</name>
  <files>src/components/accounting/ap-workflow.tsx</files>
  <action>
    **New file: `src/components/accounting/ap-workflow.tsx`** — Client component implementing the AP management interface for ACCT-10:

    **AP Dashboard section (top):**
    - 4 summary cards: Total AP Outstanding, Overdue Amount, Due This Week, Due This Month. Data from `getApSummary()`.

    **Vendor Bill Entry form:**
    - Select vendor (from existing vendors list created in Plan 01/10 — autocomplete dropdown).
    - "Add New Vendor" inline option if vendor doesn't exist yet.
    - Bill number (optional), bill date, due date, description, amount (controlled decimal input per MEMORY.md), expense category (maps to chart of accounts — dropdown of expense-type accounts).
    - "Save Bill" creates the vendor bill and auto-generates the AP journal entry.

    **Bills List (main content):**
    - Filterable table of vendor bills. Columns: Vendor, Bill #, Date, Due Date, Amount, Status badge (unpaid=amber, scheduled=blue, paid=green, overdue=red).
    - Filter chips: All, Unpaid, Scheduled, Overdue, Paid.
    - Sort by due date (soonest first by default).
    - Per-bill actions: Schedule Payment (date picker), Record Payment (payment method selector + optional reference number), Edit (unpaid only).
    - Overdue bills highlighted with red left border.

    **AP Aging Report section:**
    - Table showing aging buckets: Current, 1-30 Days, 31-60 Days, 61-90 Days, 90+ Days.
    - Each bucket shows count of bills, total amount. Expandable to show individual bills.
    - Grand total row at bottom.
    - Visual bar chart (recharts, hex colors) showing bucket distribution.

    Wire ApWorkflow into the Accounting page as a new tab "Accounts Payable" — visible in both simplified and accountant mode (AP is a core business function, not just an accounting detail). Per CONTEXT: "full AP workflow (vendor bills, scheduled payments, bank matching) for recurring supplier relationships."
  </action>
  <verify>
    1. `npm run build` passes
    2. Vendor bill form creates bill and shows in list
    3. AP aging report renders with correct bucket amounts
    4. Schedule/record payment flow updates bill status
    5. Overdue bills are visually highlighted
  </verify>
  <done>
    Full AP workflow UI: vendor bill entry with auto journal entries, bill list with status filtering, payment scheduling and recording, AP aging report with bucket breakdown and chart. Integrated into the Accounting page as an "Accounts Payable" tab accessible in both simplified and accountant modes.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Sales tax rates save and appear in tax report
- Closing a period prevents posting entries to that date range
- Reopening a period allows posting again
- Audit trail shows all journal entries with reversal links
- Vendor bills can be entered with due dates and tracked through payment
- AP aging report shows correct overdue amounts per time bucket
- Payment scheduling and recording update bill status and generate journal entries
</verification>

<success_criteria>
- Sales tax tracking per jurisdiction with configurable rates
- Period close prevents backdated modifications
- Immutable audit trail with reversal tracking
- Vendor bills entered with due dates, descriptions, and expense categories
- Payments schedulable and recordable against vendor bills
- AP aging report shows overdue amounts by 30/60/90+ day buckets
- All components wired into accounting page
</success_criteria>

<output>
After completion, create `.planning/phases/11-payroll-team-management-full-accounting/11-12-SUMMARY.md`
</output>
