---
phase: 07-billing-payments
plan: "08"
type: execute
wave: 6
depends_on: ["07-05", "07-06", "07-07"]
files_modified: []
autonomous: false
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05
  - BILL-06
  - BILL-07
  - BILL-08
  - BILL-09
  - BILL-10

must_haves:
  truths:
    - "All billing models generate correct invoices (per-stop, flat-rate, plus-chemicals, custom)"
    - "Invoice delivery works via email and SMS"
    - "Stripe Connect onboarding completes successfully"
    - "Payment page shows branded UI with card and ACH options"
    - "Surcharge disclosed before payment confirmation"
    - "Webhook marks invoice paid only after settlement"
    - "AutoPay charges saved method on invoice generation"
    - "Dunning sequence fires on configured schedule"
    - "QBO sync pushes invoices and payments"
    - "AR aging report shows correct bucket breakdowns"
    - "CSV export downloads valid data"
    - "Overdue flags visible on customer profiles and route stops"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for Phase 7: Billing & Payments. Verify all billing, payment, sync, and reporting features work correctly across all requirement areas.

Purpose: Ensure all 10 requirements (BILL-01 through BILL-10) are satisfied before moving to Phase 8.
Output: Verification report documenting pass/fail status for each test scenario.
</objective>

<execution_context>
@/Users/aaronmattia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronmattia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-payments/07-CONTEXT.md
@.planning/phases/07-billing-payments/07-01-SUMMARY.md
@.planning/phases/07-billing-payments/07-02-SUMMARY.md
@.planning/phases/07-billing-payments/07-03-SUMMARY.md
@.planning/phases/07-billing-payments/07-04-SUMMARY.md
@.planning/phases/07-billing-payments/07-05-SUMMARY.md
@.planning/phases/07-billing-payments/07-06-SUMMARY.md
@.planning/phases/07-billing-payments/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — build, type check, and schema validation</name>
  <files></files>
  <action>
    Run automated checks to verify Phase 7 implementation integrity:

    1. **TypeScript compilation**: `npx tsc --noEmit` — must pass with zero errors.
    2. **Next.js build**: `npm run build` — must succeed.
    3. **Schema verification**: Run `npx drizzle-kit push --dry-run` to verify schema is in sync.
    4. **RLS policy verification**: Execute SQL query against all Phase 7 tables to verify no NULL policies:
       ```sql
       SELECT tablename, policyname, qual, with_check
       FROM pg_catalog.pg_policies
       WHERE tablename IN ('payment_records', 'dunning_config')
       AND (qual IS NULL OR with_check IS NULL);
       ```
       If any results, RLS is broken and must be fixed.
    5. **Key file existence check**: Verify all critical Phase 7 files exist:
       - src/lib/db/schema/payments.ts
       - src/lib/db/schema/dunning.ts
       - src/lib/stripe/client.ts
       - src/lib/stripe/webhook-handlers.ts
       - src/lib/qbo/client.ts
       - src/lib/qbo/mappers.ts
       - src/lib/pay-token.ts
       - src/lib/emails/invoice-email.tsx
       - src/lib/emails/dunning-email.tsx
       - src/actions/billing.ts
       - src/actions/payments.ts
       - src/actions/dunning.ts
       - src/actions/qbo-sync.ts
       - src/actions/reports.ts
       - src/app/pay/[token]/page.tsx
       - src/app/api/webhooks/stripe/route.ts
       - src/app/api/webhooks/qbo/route.ts
       - src/app/(app)/reports/page.tsx
    6. **Export verification**: Check that key exports exist:
       - billing.ts exports generateInvoiceForCustomer, generateAllInvoices
       - payments.ts exports recordManualPayment, chargeAutoPay
       - reports.ts exports getArAging, getRevenueByCustomer, exportFinancialCsv
       - webhook-handlers.ts exports handlePaymentSucceeded, handlePaymentFailed
    7. **Key link verification**: Grep for critical integration points:
       - Payment page fetches PaymentIntent: `api/pay.*intent`
       - Webhook imports handlers: `handlePayment`
       - Webhook updates invoice status: `invoices.*paid`
       - QBO sync called from actions: `syncInvoiceToQbo|syncPaymentToQbo`
       - AR aging query with CASE buckets: `aging_bucket|days_overdue`
       - Overdue balance on customer profile: `overdue_balance`

    Document all results in the verification report.
  </action>
  <verify>
    All automated checks pass.
    No NULL RLS policies.
    All critical files exist.
    All key exports present.
    All key links verified.
  </verify>
  <done>
    Automated verification complete — build passes, types compile, schema in sync, RLS valid, all files and exports present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of all Phase 7 features</name>
  <files></files>
  <action>
    Present the user with the following verification checklist. Complete Phase 7: Billing and Payments implementation includes:
    - Multi-model invoicing (per-stop, flat-rate, plus-chemicals, custom)
    - Bulk invoice generation
    - Invoice delivery via email (with PDF) and SMS
    - Quote SMS delivery
    - Stripe Connect onboarding
    - Payment stack selector (Stripe / QBO / Both / None)
    - Credit card surcharge configuration with legal disclaimer
    - Branded public payment page with card and ACH
    - Stripe webhook handler (payment succeeded/failed, account updated, charge refunded)
    - Manual payment recording (check, cash)
    - AutoPay (save method, auto-charge on invoice generation)
    - Configurable dunning engine (retry schedule, reminder emails)
    - QuickBooks Online bidirectional sync
    - AR aging report, revenue report, P&L report
    - CSV export for tax prep
    - Overdue flags on customer profiles and route stops

    Test each requirement area. Use Stripe test mode for payment testing.

    **BILL-01 and BILL-02: Invoicing**
    1. Navigate to a customer profile. Set their billing model to "Per Stop".
    2. Go to invoicing. Generate an invoice for a billing period. Verify line items match completed stops.
    3. Change another customer to "Monthly Flat Rate" with a $150 amount. Generate invoice. Verify single line item.
    4. Test "Generate All Invoices" button. Verify all due customers get draft invoices with sequential numbers.
    5. Verify plus-chemicals model auto-populates chemical line items from visit data.

    **BILL-02: Invoice Delivery**
    6. Send an invoice via email. Verify email arrives with PDF attachment and "Pay Now" link.
    7. If customer has phone, send via SMS. Verify text message received with payment link.
    8. Test batch "Send All" for multiple invoices.
    9. Test quote SMS delivery from the quote builder.

    **BILL-03 and BILL-08: Payments and Surcharge**
    10. Open the payment link from the invoice email. Verify branded page with company logo/colors.
    11. Verify card and ACH payment options available.
    12. If surcharge enabled, verify surcharge line item and disclosure text visible before payment.
    13. Complete a test payment with Stripe test card (4242 4242 4242 4242). Verify success state.
    14. Check that invoice is NOT marked paid immediately. Wait for webhook — then verify paid status.

    **BILL-10: Stripe Connect**
    15. Go to Settings > Billing. Click "Connect Stripe Account". Verify Stripe onboarding flow loads.
    16. Verify payment stack selector (Stripe / QBO / Both / None) saves correctly.

    **BILL-04: AutoPay**
    17. On the payment page, check "Enable AutoPay" and complete a payment.
    18. Generate a new invoice for that customer. Verify auto-charge fires (check payment_records).
    19. Verify invoice stays "pending" until webhook confirms.

    **BILL-05: Dunning**
    20. Go to Settings > Billing > Dunning. Verify default 3-step dunning sequence.
    21. Modify step day offsets and save. Verify settings persist.
    22. Create an overdue invoice (set due_date in the past). Manually trigger dunning scan or wait for cron.
    23. Verify dunning email sent with correct step number and pay link.

    **BILL-06 and BILL-10: QBO Sync**
    24. Go to Settings > Billing > QBO. Click "Connect QuickBooks". Complete OAuth flow.
    25. Create a new customer. Verify it appears in QBO.
    26. Create and send an invoice. Verify it appears in QBO.
    27. Record a manual payment. Verify payment appears in QBO.

    **BILL-07 and BILL-09: Reports**
    28. Navigate to /reports. Verify three tabs: AR Aging, Revenue, P&L.
    29. Check AR aging report shows overdue invoices in correct buckets.
    30. Check revenue report shows per-customer revenue with date filter.
    31. Export CSV for invoices. Verify file downloads with correct data.

    **Overdue Flags**
    32. Find a customer with overdue balance. Verify red flag on their profile page.
    33. Check the same customer's stops on the Schedule page. Verify overdue indicator.
  </action>
  <verify>
    User confirms all 33 test scenarios pass or identifies specific issues.
  </verify>
  <done>
    All Phase 7 requirements verified. User has approved the implementation.
    Resume signal: Type "approved" to complete Phase 7, or describe any issues found.
  </done>
</task>

</tasks>

<verification>
All 10 Phase 7 requirements verified:
- BILL-01: Multi-model invoicing works
- BILL-02: Bulk generation and delivery (email + SMS) work
- BILL-03: Online payment via card and ACH works
- BILL-04: AutoPay saves method and auto-charges
- BILL-05: Dunning retries and sends reminders on schedule
- BILL-06: QBO bidirectional sync works
- BILL-07: Built-in reports (AR aging, revenue, P&L) functional
- BILL-08: Surcharge disclosed and applied correctly
- BILL-09: CSV export produces valid financial data
- BILL-10: Payment stack selector works for all configurations
</verification>

<success_criteria>
- Build and type check pass
- All critical files exist with correct exports
- All RLS policies verified non-NULL
- Human verification passes all 33 test scenarios
- No blocking issues identified
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-payments/07-08-SUMMARY.md`
</output>
